# Quicken For Mac Database Schema Reference

> Captured from Quicken 8.5 for Mac, March 2026.
> 
> **No personal financial data is included.** This document contains only structural schema information: table definitions, column names, indexes, entity types, and relationships.

## Core Data Conventions

Quicken for Mac stores its data in a [Core Data](https://developer.apple.com/documentation/coredata) SQLite database located inside the `.quicken` bundle (`data` file). The database follows standard Core Data naming conventions:

- **Z_PK** -- integer primary key on every entity table
- **Z_ENT** -- entity type discriminator (references `Z_PRIMARYKEY.Z_ENT`)
- **Z_OPT** -- optimistic locking counter, incremented on each update
- **Z-prefix** -- all table and column names are prefixed with `Z`
- **TIMESTAMP columns** -- Core Data epoch: seconds since **2001-01-01 00:00:00 UTC**. Add `978307200` to convert to Unix epoch.
- **Z_ENT values are per-database** -- always look up entity type numbers from `Z_PRIMARYKEY` at runtime; they may differ across Quicken data files.
- **Encryption** -- Quicken encrypts the `data` file when the app is closed. The database is only readable while Quicken is running.

### Writing back to the database (forks / one-off scripts)

This MCP server opens the database read-only and never writes. If you fork the schema knowledge here for an enrichment workflow that *does* write (e.g., the [Quicken ↔ Amazon order matcher gist](https://gist.github.com/Als-Pal/3fc9c18949c826c207559939e8d9b90a) that inspired the `split_note` column), Core Data has two non-obvious requirements:

1. **Increment `Z_OPT` on every UPDATE.** It is the optimistic-locking counter Core Data uses to detect concurrent edits. Updates that don't bump `Z_OPT` may be silently rolled back or cause Quicken to corrupt the row on next sync. Always write `SET ZNOTE = ?, Z_OPT = Z_OPT + 1`.
2. **Quicken must be closed before writing.** The same encryption that requires Quicken to be open for *reads* means concurrent writes from your script while Quicken is running will fight Core Data's in-memory state. Quit Quicken, run your update, then reopen — Quicken will pick up the changed rows.

Always make a file-level backup of the `.quicken/data` file before writing.

---

## Entity Types (Z_PRIMARYKEY)

The `Z_PRIMARYKEY` table maps entity names to `Z_ENT` integer discriminators. Multiple entities share single physical tables via `Z_ENT` (e.g., `ZTRANSACTION` holds both `CashFlowTransaction` and `InvestmentTransaction`; `ZTAG` holds `CategoryTag`, `UserTag`, and `CashFlowTag`).

There are **84 entity types** in total. Grouped by functional area:

### Core Financial

- `Account`
- `Transaction` -- abstract base; not directly instantiated
- `CashFlowTransaction` -- checking, credit card, savings transactions (Z_ENT varies)
- `SmartCashFlowTransaction` -- scheduled/recurring transactions
- `InvestmentTransaction` -- buy, sell, dividend, etc.
- `CashFlowTransactionEntry` -- split line items within a transaction
- `CashFlowAction`
- `UserPayee`
- `UserPayeeAddress`
- `Tag` -- abstract base for all tag types
- `CategoryTag` -- income/expense categories (e.g., Groceries, Salary)
- `UserTag` -- user-created tags for cross-cutting classification
- `CashFlowTag` -- system-managed tags for cash flow tracking
- `ReconciledTransaction`
- `ReconcileRecord`
- `AccountingRecord`
- `LoanSplitEntry`
- `ScheduleAdjustmentEntry`
- `DisplayValueLine`

### Investments

- `Security`
- `SecurityID`
- `SecurityQuote`
- `SecurityQuoteDetail`
- `SecuritySplit`
- `Position` -- investment holdings (security + account)
- `FIPosition`
- `Lot` -- tax lot for cost basis tracking
- `LotAssignment`
- `LotMod`
- `CapitalGainsEstimatorScenario`
- `ForexQuote`

### Online Banking

- `FinancialInstitution`
- `CloudFILogin`
- `FIStatement`
- `FIBalance`
- `FITransaction`
- `FIStateOFX`
- `DownloadSession`
- `ConnectivityEventResult`
- `OFXEmail`

### Bill Pay

- `BillPayFundingAccount`
- `BillPresentmentAccount`
- `BPFinancialInstitution`
- `BPFIPayee`
- `CheckPayCheck`
- `CheckPayPayee`
- `IFSBillPayBillerAccount`
- `IFSBillPayPayment`
- `IFSPresentmentBillerAccount`
- `QuickPayPayment`

### Budgets & Reports

- `Budget`
- `BudgetLineItem`
- `BudgetTarget`
- `Report`
- `ReportFolder`
- `SmartRegister`

### Business & Invoicing

- `Business`
- `Customer`
- `Invoice`
- `InvoiceLineItem`
- `CustomerCredit`
- `CustomerCreditAllocation`
- `CustomerCreditLineItem`
- `CustomerPayment`
- `CustomerPaymentAllocation`
- `CustomerRefund`
- `SalesTaxAgency`
- `SalesTaxPayment`
- `SalesTaxRate`
- `ProductService`
- `Project`
- `MileageEntry`
- `TaxPaymentRecord`

### Configuration & Metadata

- `DocumentProperty`
- `QuickFillRule`
- `QuickFillRuleSplitEntry`
- `RenamingRule`
- `Alert`
- `Attachment`
- `QError`
- `QObject`
- `SyncError`
- `CloudSyncData`
- `TaxLineItem`

---

## Table Schemas

Every table in the database is listed below with its full `CREATE TABLE` statement, grouped by functional area.

### Core Financial

#### ZACCOUNT

```sql
CREATE TABLE ZACCOUNT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZACTIVE INTEGER,
    ZAUTORECONCILE INTEGER,
    ZCANDIDATENOCASHTRANSACTIONS INTEGER,
    ZCANDIDATESINGLEMUTUALFUND INTEGER,
    ZCLOSED INTEGER,
    ZCONFIRMATIONNEEDED INTEGER,
    ZCOSTBASISALGORITHMDEBT INTEGER,
    ZCOSTBASISALGORITHMMUTUALFUND INTEGER,
    ZCOSTBASISALGORITHMOPTION INTEGER,
    ZCOSTBASISALGORITHMOTHER INTEGER,
    ZCOSTBASISALGORITHMSTOCK INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZIMPORTQID INTEGER,
    ZLASTCHECKNUM INTEGER,
    ZLIQUIDITY INTEGER,
    ZLOANCOMPOUNDINGPERIOD INTEGER,
    ZLOANHASBEENSETUP INTEGER,
    ZLOANHASOFFEREDTOCONVERTLIABILITY INTEGER,
    ZLOANPAYMENTFREQUENCY INTEGER,
    ZLOANPAYMENTPERIOD INTEGER,
    ZLOANRATETYPE INTEGER,
    ZLOANTERMTYPE INTEGER,
    ZLOANUSEMATURITYDATE INTEGER,
    ZMOSTRECENTDOWNLOADSESSIONNUMBER INTEGER,
    ZNOCASHTRANSACTIONS INTEGER,
    ZOFXPAYMENTSENABLED INTEGER,
    ZOFXTRANSFERDESTENABLED INTEGER,
    ZOFXTRANSFERSOURCEENABLED INTEGER,
    ZONLINEBANKINGACCOUNTINFOSESSIONNEEDED INTEGER,
    ZONLINEBANKINGACCOUNTISENABLED INTEGER,
    ZONLINEBANKINGACCOUNTISINBIDCHANGE INTEGER,
    ZONLINEBANKINGAGGREGATIONDISABLEMENTCOUNT INTEGER,
    ZONLINEBANKINGAGGREGATIONENABLEMENTCOUNT INTEGER,
    ZONLINEBANKINGAWAITINGINITIALDOWNLOAD INTEGER,
    ZONLINEBANKINGCHANGEPINFIRSTCOMPLETED INTEGER,
    ZQUICKENID INTEGER,
    ZSCHEDULELOOKAHEADVALUE INTEGER,
    ZSIMPLEINVESTING INTEGER,
    ZSINGLEMUTUALFUND INTEGER,
    ZSTATUSTRANSACTIONCOUNT INTEGER,
    ZSYNCRESETREQUIRED INTEGER,
    ZSYNCWITHCLOUD INTEGER,
    ZTAXABLE INTEGER,
    ZUSEDINAUTOUPDATES INTEGER,
    ZUSEDINREPORTS INTEGER,
    ZBILLPAYFUNDINGACCOUNT INTEGER,
    ZBILLPAYPROVIDERINSTITUTION INTEGER,
    ZBPSACCOUNT INTEGER,
    ZCLOUDFILOGIN INTEGER,
    ZERROR INTEGER,
    ZFINANCIALINSTITUTION INTEGER,
    ZLOANINTERESTCATEGORY INTEGER,
    ZLOANSMARTTRANSACTION INTEGER,
    ZPRIMARYUSEBUSINESS INTEGER,
    ZCLOSEDDATE TIMESTAMP,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZLOANFIRSTPAYMENTDATE TIMESTAMP,
    ZLOANNEXTPAYMENTDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZONLINEBANKINGLASTCONNECTEDTIMESTAMP TIMESTAMP,
    ZONLINEBANKINGLEDGERBALANCEDATE TIMESTAMP,
    ZREVIEWEDTIMESTAMP TIMESTAMP,
    ZSHADOWACCOUNTCREATIONABANDONEDATTIMESTAMP TIMESTAMP,
    ZSTATEMENTCLOSETIMESTAMP TIMESTAMP,
    ZSTATEMENTDUETIMESTAMP TIMESTAMP,
    ZSTATEMENTENDTIMESTAMP TIMESTAMP,
    ZSTATEMENTLASTPAYMENTTIMESTAMP TIMESTAMP,
    ZSTATUSTRANSACTIONCOUNTTIMESTAMP TIMESTAMP,
    ZUSERMODIFIEDTIMESTAMP TIMESTAMP,
    ZCREDITLIMIT DECIMAL,
    ZINTERESTRATE DECIMAL,
    ZLOANOPENINGBALANCE DECIMAL,
    ZLOANTERM DECIMAL,
    ZONLINEBANKINGLEDGERBALANCEAMOUNT DECIMAL,
    ZSTATEMENTAMOUNTDUE DECIMAL,
    ZSTATEMENTAMOUNTPASTDUE DECIMAL,
    ZSTATEMENTCLOSINGBALANCE DECIMAL,
    ZSTATEMENTLASTPAYMENTAMOUNT DECIMAL,
    ZSTATEMENTMINIMUMAMOUNTDUE DECIMAL,
    ZBANKROUTINGNUMBER VARCHAR,
    ZBILLPAYROUTINGNUMBER VARCHAR,
    ZCHECKPRINTINGPROPERTIESJSON VARCHAR,
    ZCURRENCY VARCHAR,
    ZDATASOURCE VARCHAR,
    ZFILTERSJSON VARCHAR,
    ZGUID VARCHAR,
    ZIMPORTSOURCEFILEID VARCHAR,
    ZIMPORTSOURCEID VARCHAR,
    ZNAME VARCHAR,
    ZNOTES VARCHAR,
    ZNOTIFICATIONSJSON VARCHAR,
    ZOFXBILLPAYSERVICESTATUS VARCHAR,
    ZOFXLASTBANKMAILTOKEN VARCHAR,
    ZOFXLASTPAYMENTTOKEN VARCHAR,
    ZOFXLASTTRANSFERTOKEN VARCHAR,
    ZONLINEBANKINGACCOUNTDESCRIPTION VARCHAR,
    ZONLINEBANKINGACCOUNTNUMBER VARCHAR,
    ZONLINEBANKINGACCOUNTNUMBERMASKED VARCHAR,
    ZONLINEBANKINGAGGREGATORACCOUNTID VARCHAR,
    ZONLINEBANKINGCONNECTIONTYPE VARCHAR,
    ZONLINEBANKINGCUSTOMERID VARCHAR,
    ZONLINEBANKINGDTEND VARCHAR,
    ZONLINEBANKINGOFXACCOUNTTYPE VARCHAR,
    ZONLINEBANKINGPREVIOUSBID VARCHAR,
    ZONLINEBANKINGSERVERID VARCHAR,
    ZONLINEBILLPAYACCOUNTNUMBER VARCHAR,
    ZONLINEBILLPAYCONNECTIONTYPE VARCHAR,
    ZONLINEBILLPAYCUSTOMERID VARCHAR,
    ZONLINEBILLPAYOFXACCOUNTTYPE VARCHAR,
    ZPRIMARYUSE VARCHAR,
    ZQ17SYNCID VARCHAR,
    ZREALESTATEADDRESSCITY VARCHAR,
    ZREALESTATEADDRESSCOUNTRY VARCHAR,
    ZREALESTATEADDRESSLINE1 VARCHAR,
    ZREALESTATEADDRESSLINE2 VARCHAR,
    ZREALESTATEADDRESSSTATE VARCHAR,
    ZREALESTATEADDRESSZIP VARCHAR,
    ZRECONCILEINFOJSON VARCHAR,
    ZREGIONCODE VARCHAR,
    ZSCHEDULELOOKAHEADUNITTYPE VARCHAR,
    ZSYNCID VARCHAR,
    ZTYPENAME VARCHAR,
    ZUSERDESCRIPTION VARCHAR,
    ZANOMALOUSSYNCSTATUS BLOB,
    ZCHECKPRINTINGPROPERTIES BLOB,
    ZFILTERS BLOB,
    ZINCOMEFILTERS BLOB,
    ZNOTIFICATIONS BLOB,
    ZONLINEBANKINGAGGREGATIONPENDINGENABLEMENTINFO BLOB,
    ZRECONCILEINFO BLOB,
    ZSPENDINGFILTERS BLOB,
    ZBALANCECHARTFILTERS BLOB,
    ZSHOWCREDITLIMITLINEINCHART INTEGER,
    ZCREDITLIMITLINECOLORSTRING VARCHAR,
    ZREALESTATELASTUSEDUPDATEMETHOD VARCHAR
)
```

#### ZTRANSACTION

```sql
CREATE TABLE ZTRANSACTION (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZEXCLUDEFROMREPORTS INTEGER,
    ZIMPORTQID INTEGER,
    ZLOCKED INTEGER,
    ZMATCHED INTEGER,
    ZORDERID INTEGER,
    ZQUICKENID INTEGER,
    ZRECONCILESTATUS INTEGER,
    ZTYPE INTEGER,
    ZUSERSTATUSOVERRIDE INTEGER,
    ZACCOUNT INTEGER,
    ZACCOUNTINGRECORD INTEGER,
    ZDOWNLOADSESSION INTEGER,
    ZERROR INTEGER,
    ZFITRANSACTION INTEGER,
    ZPLACEHOLDERACCOUNT INTEGER,
    ZRECONCILEDTRANSACTION INTEGER,
    ZRENAMINGRULEUSED INTEGER,
    ZSENDACCOUNT INTEGER,
    ZTARGETACCOUNT INTEGER,
    ZFLAG INTEGER,
    ZMACHINEGENERATEDCATEGORYSOURCE INTEGER,
    ZMACHINEGENERATEDPAYEESOURCE INTEGER,
    ZMACHINEGENERATEDTAGSOURCE INTEGER,
    ZMISCFLAGS INTEGER,
    ZSENDREQUESTSTATE INTEGER,
    ZSOURCESMARTCASHFLOWTRANSACTIONQUICKENID INTEGER,
    ZACTION INTEGER,
    ZCHECKPAYCHECK INTEGER,
    ZIFSPAYMENT INTEGER,
    ZQUICKPAYPAYMENT INTEGER,
    ZUSERPAYEE INTEGER,
    ZAUTOPOST INTEGER,
    ZEDITBEFOREMARKINGASPAID INTEGER,
    ZIFSPAYMENTTOBESENT INTEGER,
    ZLOANACCOUNT INTEGER,
    ZPRESENTMENTBILLERACCOUNT INTEGER,
    ZCOSTBASISALGORITHM INTEGER,
    ZTAXEXEMPT INTEGER,
    ZPOSITION INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZENTEREDDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZPLACEHOLDERTARGETDATE TIMESTAMP,
    ZPOSTEDDATE TIMESTAMP,
    ZUSERMODIFIEDTIMESTAMP TIMESTAMP,
    ZBUDGETFLEXIBILITY FLOAT,
    ZSENTTRANSACTIONSTATUSDATE TIMESTAMP,
    ZSOURCESMARTCASHFLOWTRANSACTIONORIGINALMODELDATE TIMESTAMP,
    ZBILLDATE TIMESTAMP,
    ZBILLDUEDATE TIMESTAMP,
    ZBILLSNAPSHOTUPDATETIMESTAMP TIMESTAMP,
    ZACQUISITIONDATE TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZPLACEHOLDERTARGETBALANCE DECIMAL,
    ZBILLCURRENTBALANCE DECIMAL,
    ZBILLMINIMUMAMOUNTDUE DECIMAL,
    ZBILLTOTALAMOUNTDUE DECIMAL,
    ZCOMMISSION DECIMAL,
    ZCOSTBASIS DECIMAL,
    ZDENOMINATOR DECIMAL,
    ZNUMERATOR DECIMAL,
    ZPLACEHOLDERCOSTBASIS DECIMAL,
    ZPLACEHOLDERCOSTBASISUNITS DECIMAL,
    ZPLACEHOLDERTARGETUNITS DECIMAL,
    ZPLACEHOLDERUNITSONTARGETDATE DECIMAL,
    ZUNITS DECIMAL,
    ZDATASOURCE VARCHAR,
    ZFINOTE VARCHAR,
    ZGUID VARCHAR,
    ZIMPORTSOURCEFILEID VARCHAR,
    ZIMPORTSOURCEID VARCHAR,
    ZMATCHSOURCE VARCHAR,
    ZNOTE VARCHAR,
    ZONLINEBANKINGSERVERID VARCHAR,
    ZPREMATCHPROPERTIESJSON VARCHAR,
    ZREFERENCE VARCHAR,
    ZSYNCID VARCHAR,
    ZCHECKNUMBER VARCHAR,
    ZCHECKPROPERTIESJSON VARCHAR,
    ZCLIENTASSIGNEDTRANSACTIONUID VARCHAR,
    ZOFXPAYEELISTID VARCHAR,
    ZSOURCEMEMRULEGUID VARCHAR,
    ZBILLERENROLLMENTSTATUSSTRING VARCHAR,
    ZBILLERTYPESTRING VARCHAR,
    ZBILLERWEBSITEURLSTRING VARCHAR,
    ZPRESENTMENTACCOUNTID VARCHAR,
    ZRECURRENCEJSON VARCHAR,
    ZLOCATION BLOB,
    ZPREMATCHPROPERTIES BLOB,
    ZCHECKPROPERTIES BLOB,
    ZAUTOADJUSTAVERAGENUMPAYMENTS INTEGER,
    ZAUTOADJUSTAMOUNTTYPE VARCHAR
)
```

#### ZCASHFLOWTRANSACTIONENTRY

```sql
CREATE TABLE ZCASHFLOWTRANSACTIONENTRY (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZBILLABLE INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZMISCFLAGS INTEGER,
    ZQUICKENID INTEGER,
    ZSEQUENCENUMBER INTEGER,
    ZBUSINESS INTEGER,
    ZCATEGORYTAG INTEGER,
    ZCUSTOMER INTEGER,
    ZCUSTOMERPAYMENT INTEGER,
    ZBILLEDINVOICELINEITEM INTEGER,
    ZPARENT INTEGER,
    Z79_PARENT INTEGER,
    ZPROJECT INTEGER,
    ZSALESTAXPAYMENT INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZIMPORTSOURCEFILEID VARCHAR,
    ZIMPORTSOURCEID VARCHAR,
    ZNOTE VARCHAR,
    ZSYNCID VARCHAR,
    ZTRANSFER VARCHAR,
    ZCUSTOMERREFUND INTEGER,
    ZCUSTOMERCREDITLINEITEM INTEGER,
    ZTYPESTRING VARCHAR,
    ZINVOICELINEITEM INTEGER
)
```

#### ZCASHFLOWACTION

```sql
CREATE TABLE ZCASHFLOWACTION (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZRETIRED INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZIMPORTSOURCEFILEID VARCHAR,
    ZNAME VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZUSERPAYEE

```sql
CREATE TABLE ZUSERPAYEE (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZEBILLENABLED INTEGER,
    ZINFOCHANGED INTEGER,
    ZQUICKENID INTEGER,
    ZSHOWINBILLCENTER INTEGER,
    ZVISIBILITYTYPE INTEGER,
    ZBPSACCOUNT INTEGER,
    ZCHECKPAYADDRESS INTEGER,
    ZIFSBILLERACCOUNT INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZLASTSELECTEDTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZURL VARCHAR,
    ZADDRESS VARCHAR,
    ZADDRESS1 VARCHAR,
    ZADDRESS2 VARCHAR,
    ZADDRESS3 VARCHAR,
    ZCITY VARCHAR,
    ZCOUNTRY VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZIMPORTSOURCEFILEID VARCHAR,
    ZINTERNALNAME VARCHAR,
    ZNAME VARCHAR,
    ZPHONENUMBER VARCHAR,
    ZPOSTALCODE VARCHAR,
    ZSTATE VARCHAR,
    ZSYNCID VARCHAR,
    ZUSERACCOUNTNUMBER VARCHAR
)
```

#### ZUSERPAYEEADDRESS

```sql
CREATE TABLE ZUSERPAYEEADDRESS (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZHASBEENVERIFIED INTEGER,
    ZQUICKENID INTEGER,
    ZUSERPAYEE INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZACCOUNTNUMBER VARCHAR,
    ZCITY VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZLINE1 VARCHAR,
    ZLINE2 VARCHAR,
    ZSTATE VARCHAR,
    ZSYNCID VARCHAR,
    ZZIPCODE VARCHAR
)
```

#### ZTAG

```sql
CREATE TABLE ZTAG (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZBUDGETABLE INTEGER,
    ZEDITABLE INTEGER,
    ZFAVORITE INTEGER,
    ZGROUPING INTEGER,
    ZHIDDEN INTEGER,
    ZIMPORTQID INTEGER,
    ZTAXCOPYNUM INTEGER,
    ZTAXREFCA INTEGER,
    ZTAXREFUS INTEGER,
    ZTRENDABLE INTEGER,
    ZTYPE INTEGER,
    ZUSERASSIGNABLE INTEGER,
    ZCATEGORYID INTEGER,
    ZCUSTOM INTEGER,
    ZPARENTCATEGORY INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZUSERMODIFIEDTIMESTAMP TIMESTAMP,
    ZFLEXIBILITY FLOAT,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZINTERNALNAME VARCHAR,
    ZNAME VARCHAR,
    ZSYNCID VARCHAR,
    ZUSERDESCRIPTION VARCHAR,
    ZIMPORTSOURCECLASS VARCHAR,
    ZIMPORTSOURCEFILEID VARCHAR,
    ZIMPORTSOURCEID VARCHAR,
    ZCOLORSTRING VARCHAR,
    ZQCSKNOWNCATEGORYIDS BLOB,
    ZTAXREFMAPPINGSCA BLOB,
    ZTAXREFMAPPINGSUS BLOB
)
```

#### ZRECONCILEDTRANSACTION

```sql
CREATE TABLE ZRECONCILEDTRANSACTION (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZADJUSTMENTTYPE INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZORIGINALTRANSACTION INTEGER,
    Z79_ORIGINALTRANSACTION INTEGER,
    ZRECONCILERECORD INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZENTEREDDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZPOSTEDDATE TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR,
    ZUSERPAYEENAME VARCHAR,
    ZFITRANSACTION INTEGER,
    ZISUNCLEAREDTRANSACTION INTEGER
)
```

#### ZRECONCILERECORD

```sql
CREATE TABLE ZRECONCILERECORD (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZACCOUNT INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZENDDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZBEGINNINGBALANCE DECIMAL,
    ZENDINGBALANCE DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR,
    ZSUPPORTSUNCLEAREDTRANSACTIONS INTEGER
)
```

#### ZACCOUNTINGRECORD

```sql
CREATE TABLE ZACCOUNTINGRECORD (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZBUSINESS INTEGER,
    ZCUSTOMERPAYMENT INTEGER,
    ZINVOICE INTEGER,
    ZSALESTAXPAYMENT INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR,
    ZTYPESTRING VARCHAR,
    ZCUSTOMERCREDIT INTEGER,
    ZCUSTOMERREFUND INTEGER
)
```

#### ZLOANSPLITENTRY

```sql
CREATE TABLE ZLOANSPLITENTRY (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZMISCFLAGS INTEGER,
    ZQUICKENID INTEGER,
    ZSEQUENCENUMBER INTEGER,
    ZACCOUNT INTEGER,
    ZCATEGORY INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR,
    ZTRANSFER VARCHAR
)
```

#### ZSCHEDULEADJUSTMENTENTRY

```sql
CREATE TABLE ZSCHEDULEADJUSTMENTENTRY (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZPARENT INTEGER,
    ZADJUSTEDDATE TIMESTAMP,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZORIGINALSCHEDULEDDATE TIMESTAMP,
    ZADJUSTEDAMOUNT DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZDISPLAYVALUELINE

```sql
CREATE TABLE ZDISPLAYVALUELINE (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZISHIDDEN INTEGER,
    ZQUICKENID INTEGER,
    ZACCOUNT INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZVALUE DECIMAL,
    ZCOLORSTRING VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZLINENAME VARCHAR,
    ZNAME VARCHAR,
    ZSYNCID VARCHAR
)
```

### Investments

#### ZSECURITY

```sql
CREATE TABLE ZSECURITY (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZASSETMIXTUREDATAISUSERDEFINED INTEGER,
    ZCOSTBASISALGORITHM INTEGER,
    ZDEBTRATINGTYPE INTEGER,
    ZDEBTTYPE INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZHIDEINLISTS INTEGER,
    ZINTERESTFREQUENCYTYPE INTEGER,
    ZQUICKENID INTEGER,
    ZSHOULDDOWNLOADQUOTES INTEGER,
    ZTAXFREE INTEGER,
    ZTYPE INTEGER,
    ZWATCHLIST INTEGER,
    ZCOUPONMATURITYDATE TIMESTAMP,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZDEBTCALLDATE TIMESTAMP,
    ZDEBTMATURITYDATE TIMESTAMP,
    ZEXPIREDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZMOSTRECENTQUOTEDOWNLOADTIMESTAMP TIMESTAMP,
    ZSYNCMODIFICATIONTIMESTAMP TIMESTAMP,
    ZASSETCLASSPERCENTAGECASH DECIMAL,
    ZASSETCLASSPERCENTAGEDOMESTICBOND DECIMAL,
    ZASSETCLASSPERCENTAGEINTLBOND DECIMAL,
    ZASSETCLASSPERCENTAGEINTLSTOCK DECIMAL,
    ZASSETCLASSPERCENTAGELARGESTOCK DECIMAL,
    ZASSETCLASSPERCENTAGEMONEYMRKT DECIMAL,
    ZASSETCLASSPERCENTAGEOTHER DECIMAL,
    ZASSETCLASSPERCENTAGESMALLSTOCK DECIMAL,
    ZBACKLOADFEERATE DECIMAL,
    ZCOUPONRATE DECIMAL,
    ZDEBTMATURITYRATE DECIMAL,
    ZFACEVALUEUNITPRICE DECIMAL,
    ZSHARESPERCONTRACT DECIMAL,
    ZSTRIKEPRICE DECIMAL,
    ZASSETCLASS VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGOALSTRING VARCHAR,
    ZGUID VARCHAR,
    ZISSUETYPE VARCHAR,
    ZNAME VARCHAR,
    ZOPTIONTYPE VARCHAR,
    ZSYNCID VARCHAR,
    ZTICKER VARCHAR
)
```

#### ZSECURITYID

```sql
CREATE TABLE ZSECURITYID (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZSECURITY INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR,
    ZUNIQUEID VARCHAR,
    ZUNIQUEIDTYPE VARCHAR
)
```

#### ZSECURITYQUOTE

```sql
CREATE TABLE ZSECURITYQUOTE (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZSECURITY INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZQUOTEDATE TIMESTAMP,
    ZCLOSINGPRICE DECIMAL,
    ZHIGHESTPRICE DECIMAL,
    ZLOWESTPRICE DECIMAL,
    ZOPENINGPRICE DECIMAL,
    ZVOLUME DECIMAL,
    ZCHOSENDETAILDATASOURCE VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZSECURITYQUOTEDETAIL

```sql
CREATE TABLE ZSECURITYQUOTEDETAIL (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZSECURITYQUOTE INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZCLOSINGPRICE DECIMAL,
    ZHIGHESTPRICE DECIMAL,
    ZLOWESTPRICE DECIMAL,
    ZOPENINGPRICE DECIMAL,
    ZVOLUME DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZSECURITYSPLIT

```sql
CREATE TABLE ZSECURITYSPLIT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZSECURITY INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZSPLITDATE TIMESTAMP,
    ZDENOMINATOR DECIMAL,
    ZNUMERATOR DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZPOSITION

```sql
CREATE TABLE ZPOSITION (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZCOSTBASISALGORITHM INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZTYPE INTEGER,
    ZACCOUNT INTEGER,
    ZSECURITY INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR,
    ZUNIQUEID VARCHAR,
    ZUNIQUEIDTYPE VARCHAR
)
```

#### ZFIPOSITION

```sql
CREATE TABLE ZFIPOSITION (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZSECURITYTYPE INTEGER,
    ZFISTATEMENT INTEGER,
    ZCOUPONMATURITYDATE TIMESTAMP,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZDEBTMATURITYDATE TIMESTAMP,
    ZEXPIREDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZASSETCLASSPERCENTAGEDOMESTICBOND DECIMAL,
    ZASSETCLASSPERCENTAGEINTLBOND DECIMAL,
    ZASSETCLASSPERCENTAGEINTLSTOCK DECIMAL,
    ZASSETCLASSPERCENTAGELARGESTOCK DECIMAL,
    ZASSETCLASSPERCENTAGEMONEYMRKT DECIMAL,
    ZASSETCLASSPERCENTAGEOTHER DECIMAL,
    ZASSETCLASSPERCENTAGESMALLSTOCK DECIMAL,
    ZCOUPONRATE DECIMAL,
    ZMARKETVALUE DECIMAL,
    ZSECURITYUNITPRICE DECIMAL,
    ZSHARESPERCONTRACT DECIMAL,
    ZSTRIKEPRICE DECIMAL,
    ZUNITPRICE DECIMAL,
    ZUNITS DECIMAL,
    ZASSETCLASS VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZISSUETYPE VARCHAR,
    ZMEMO VARCHAR,
    ZOPTIONTYPE VARCHAR,
    ZPOSTYPE VARCHAR,
    ZSECURITYMEMO VARCHAR,
    ZSECURITYNAME VARCHAR,
    ZSECURITYTICKER VARCHAR,
    ZSECURITYTYPEDESCRIPTION VARCHAR,
    ZSYNCID VARCHAR,
    ZUNIQUEID VARCHAR,
    ZUNIQUEIDTYPE VARCHAR
)
```

#### ZLOT

```sql
CREATE TABLE ZLOT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZPOSITION INTEGER,
    ZACQUISITIONDATE TIMESTAMP,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZINITIALTRANSACTIONDATE TIMESTAMP,
    ZLATESTTRANSACTIONDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZINITIALCOSTBASIS DECIMAL,
    ZINITIALUNITS DECIMAL,
    ZLATESTCOSTBASIS DECIMAL,
    ZLATESTUNITS DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZLOTASSIGNMENT

```sql
CREATE TABLE ZLOTASSIGNMENT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZDESTINATIONTRANSACTION INTEGER,
    ZLOTMOD INTEGER,
    ZSOURCETRANSACTION INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZUNITS DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZLOTMOD

```sql
CREATE TABLE ZLOTMOD (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZTERMTYPE INTEGER,
    ZTRANSACTIONQUICKENID INTEGER,
    ZLOT INTEGER,
    ZLOTASSIGNMENT INTEGER,
    ZTRANSACTION INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZTRANSACTIONDATE TIMESTAMP,
    ZAFTERCOSTBASIS DECIMAL,
    ZAFTERPOSITIONCOSTBASIS DECIMAL,
    ZAFTERPOSITIONUNITS DECIMAL,
    ZAFTERUNITS DECIMAL,
    ZBEFORECOSTBASIS DECIMAL,
    ZBEFOREUNITS DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZCAPITALGAINSESTIMATORSCENARIO

```sql
CREATE TABLE ZCAPITALGAINSESTIMATORSCENARIO (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZAPPLYNETINVESTMENTINCOMETAX INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZFEDERALLONGTERMTAXRATE DECIMAL,
    ZFEDERALSHORTTERMTAXRATE DECIMAL,
    ZSTATELONGTERMTAXRATE DECIMAL,
    ZSTATESHORTTERMTAXRATE DECIMAL,
    ZDATASOURCE VARCHAR,
    ZFILTERSJSON VARCHAR,
    ZGUID VARCHAR,
    ZLOTSNAPSHOTSJSON VARCHAR,
    ZMAINSCENARIOOUTLINESTATEJSON VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZFOREXQUOTE

```sql
CREATE TABLE ZFOREXQUOTE (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZQUOTEDATE TIMESTAMP,
    ZEXCHANGERATE DECIMAL,
    ZMANUALEXCHANGERATE DECIMAL,
    ZDATASOURCE VARCHAR,
    ZFROMCURRENCY VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR,
    ZTOCURRENCY VARCHAR
)
```

### Online Banking

#### ZFINANCIALINSTITUTION

```sql
CREATE TABLE ZFINANCIALINSTITUTION (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDIRECTCONNECTINSTITUTIONID INTEGER,
    ZPROFILEHASRUN INTEGER,
    ZPROFILEINV401KDOWNLOAD INTEGER,
    ZPROFILEPINCHANGEALLOWED INTEGER,
    ZPROFILEPINRULECASESENSITIVE INTEGER,
    ZPROFILEPINRULEMAX INTEGER,
    ZPROFILEPINRULEMIN INTEGER,
    ZPROFILEPINRULESPACESALLOWED INTEGER,
    ZPROFILEPINRULESPECIALCHARSALLOWED INTEGER,
    ZWASACTIVEINLASTFIDIRUPDATE INTEGER,
    ZBPFINANCIALINSTITUTION INTEGER,
    ZLASTUPDATEDFROMFIDIRTIMESTAMP TIMESTAMP,
    ZBRANDINGINTU401KDNLDOPT VARCHAR,
    ZBRANDINGINTULOGINURL VARCHAR,
    ZDIRECTCONNECTBID VARCHAR,
    ZDIRECTCONNECTCONTACTPHONE VARCHAR,
    ZDIRECTCONNECTDCURL VARCHAR,
    ZDIRECTCONNECTINFOURL VARCHAR,
    ZDIRECTCONNECTORG VARCHAR,
    ZNAME VARCHAR,
    ZOFXPROCESSINGDAYSOFF VARCHAR,
    ZPROFILEPINRULECHARTYPE VARCHAR,
    ZQUICKENID INTEGER,
    ZDATASOURCE VARCHAR,
    ZDELETIONCOUNT INTEGER,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZGUID VARCHAR,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZSYNCID VARCHAR
)
```

#### ZCLOUDFILOGIN

```sql
CREATE TABLE ZCLOUDFILOGIN (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZSTATUSISTERMINAL INTEGER,
    ZFINANCIALINSTITUTION INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZCPSTATUS VARCHAR,
    ZDATASOURCE VARCHAR,
    ZDISPLAYNAME VARCHAR,
    ZGUID VARCHAR,
    ZLOGINNAME VARCHAR,
    ZPROVIDERTYPE VARCHAR,
    ZSTATUS VARCHAR,
    ZSYNCID VARCHAR,
    ZTOKENFORREFRESHJOB VARCHAR,
    ZCPCHANNEL VARCHAR,
    ZCPLOGINID VARCHAR,
    ZLASTREFRESHSUCCESSTIMESTAMP TIMESTAMP,
    ZLASTREFRESHATTEMPTTIMESTAMP TIMESTAMP
)
```

#### ZFISTATEMENT

```sql
CREATE TABLE ZFISTATEMENT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZACCOUNT INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZDATEASOF TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAVAILCASH DECIMAL,
    ZMARGINBALANCE DECIMAL,
    ZSHORTBALANCE DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZFIBALANCE

```sql
CREATE TABLE ZFIBALANCE (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZFISTATEMENT INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZBALANCEVALUE DECIMAL,
    ZBALANCEDESCRIPTION VARCHAR,
    ZBALANCETYPE VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZNAME VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZFITRANSACTION

```sql
CREATE TABLE ZFITRANSACTION (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZCCCATEGORYID INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZTYPE INTEGER,
    ZACCOUNT INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZPOSTEDDATE TIMESTAMP,
    ZACCRUEDINTEREST DECIMAL,
    ZAMOUNT DECIMAL,
    ZCOMMISSION DECIMAL,
    ZDENOMINATOR DECIMAL,
    ZFEES DECIMAL,
    ZLOAD DECIMAL,
    ZNUMERATOR DECIMAL,
    ZPENALTY DECIMAL,
    ZSTATEWITHHOLDING DECIMAL,
    ZTAXES DECIMAL,
    ZUNITS DECIMAL,
    ZWITHHOLDING DECIMAL,
    ZCCCATEGORYNAME VARCHAR,
    ZCCPAYEENAME VARCHAR,
    ZDATASOURCE VARCHAR,
    ZFITRANSACTIONID VARCHAR,
    ZGUID VARCHAR,
    ZIMPORTSOURCEFILEID VARCHAR,
    ZINCOMETYPE VARCHAR,
    ZPOSTYPE VARCHAR,
    ZSERVERTRANSID VARCHAR,
    ZSTATEMENTPAYEENAME VARCHAR,
    ZSYNCID VARCHAR,
    ZUNIQUEID VARCHAR,
    ZUNIQUEIDTYPE VARCHAR,
    ZMLKNOWNCATEGORYID VARCHAR,
    ZQCSKNOWNCATEGORYID VARCHAR,
    ZMLINFERREDPAYEE VARCHAR
)
```

#### ZFISTATEOFX

```sql
CREATE TABLE ZFISTATEOFX (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZPASSWORDCHANGEREQUIRED INTEGER,
    ZQUICKENID INTEGER,
    ZRESPONSENOTRECEIVED INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZLASTATTEMPTTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZCUSTOMERID VARCHAR,
    ZDATASOURCE VARCHAR,
    ZDIRECTCONNECTBID VARCHAR,
    ZFILEUID VARCHAR,
    ZGUID VARCHAR,
    ZLASTMAILTOKEN VARCHAR,
    ZLASTPAYEETOKEN VARCHAR,
    ZLASTPMTMAILTOKEN VARCHAR,
    ZREQUESTINERRORRECOVERY VARCHAR,
    ZRESENDURL VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZDOWNLOADSESSION

```sql
CREATE TABLE ZDOWNLOADSESSION (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZCONNECTIONMETHOD INTEGER,
    ZDOCUMENTOPENEDCOUNT INTEGER,
    ZSESSIONNUMBER INTEGER,
    ZDOWNLOADTIMESTAMP TIMESTAMP
)
```

#### ZCONNECTIVITYEVENTRESULT

```sql
CREATE TABLE ZCONNECTIVITYEVENTRESULT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZERRORDAYS INTEGER,
    ZERRORSNOOZEDAYS INTEGER,
    ZLASTAPPLIEDACTIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZREQUESTBODYLENGTH INTEGER,
    ZRESPONSEBODYLENGTH INTEGER,
    ZRESPONSESTATUSCODE INTEGER,
    ZSEQUENTIALOCCURRENCECOUNT INTEGER,
    ZSKIPPEDCOUNT INTEGER,
    ZACCOUNT INTEGER,
    ZCLOUDFILOGIN INTEGER,
    ZDOWNLOADSESSION INTEGER,
    ZSHADOWACCOUNT INTEGER,
    ZTRANSACTION INTEGER,
    Z79_TRANSACTION INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZLASTSEENTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAGGCHANNEL VARCHAR,
    ZCREATIONSIGNATURE VARCHAR,
    ZDATASOURCE VARCHAR,
    ZERRORCODE VARCHAR,
    ZERRORDETAIL VARCHAR,
    ZERRORMESSAGE VARCHAR,
    ZERRORSEVERITY VARCHAR,
    ZERRORSTATUS VARCHAR,
    ZERRORURI VARCHAR,
    ZEVALUATIONREASON VARCHAR,
    ZFIACCOUNTNAME VARCHAR,
    ZFIACCOUNTSUBTYPE VARCHAR,
    ZFIBID VARCHAR,
    ZFINAME VARCHAR,
    ZGUID VARCHAR,
    ZLASTAPPLIEDACTION VARCHAR,
    ZNAME VARCHAR,
    ZOBJECTGUID VARCHAR,
    ZOPERATIONGUID VARCHAR,
    ZOPERATIONTYPE VARCHAR,
    ZQCSTRACEID VARCHAR,
    ZREQUESTBODY VARCHAR,
    ZREQUESTURL VARCHAR,
    ZRESPONSEBODY VARCHAR,
    ZREVIEWSTATUS VARCHAR,
    ZRULERESULTSJSON VARCHAR,
    ZSESSIONCLASS VARCHAR,
    ZSESSIONID VARCHAR,
    ZSKIPPEDSUMMARY VARCHAR,
    ZSYNCID VARCHAR,
    ZSYSTEMDETAILS VARCHAR
)
```

#### ZOFXEMAIL

```sql
CREATE TABLE ZOFXEMAIL (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZACCOUNT INTEGER,
    ZFISTATEOFX INTEGER,
    ZTRANSACTION INTEGER,
    Z79_TRANSACTION INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZDTCREATED TIMESTAMP,
    ZDTUSER TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZACCOUNTNUMBER VARCHAR,
    ZACCOUNTTYPE VARCHAR,
    ZBANKID VARCHAR,
    ZCHECKNUM VARCHAR,
    ZCLIENTASSIGNEDTRANSACTIONID VARCHAR,
    ZDATASOURCE VARCHAR,
    ZEMAILTYPE VARCHAR,
    ZGUID VARCHAR,
    ZMAILFROM VARCHAR,
    ZMAILTO VARCHAR,
    ZMSGBODY VARCHAR,
    ZONLINEBANKINGSERVERID VARCHAR,
    ZPAYEELISTID VARCHAR,
    ZPAYEENAME VARCHAR,
    ZSENDSTATUS VARCHAR,
    ZSUBJECT VARCHAR,
    ZSYNCID VARCHAR,
    ZUSERID VARCHAR
)
```

### Bill Pay

#### ZBILLPAYFUNDINGACCOUNT

```sql
CREATE TABLE ZBILLPAYFUNDINGACCOUNT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZACCOUNT INTEGER,
    ZACCOUNTHOLDERSDATEOFBIRTH TIMESTAMP,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZACCOUNTHASH VARCHAR,
    ZACCOUNTHOLDERSADDRESSCITY VARCHAR,
    ZACCOUNTHOLDERSADDRESSLINE1 VARCHAR,
    ZACCOUNTHOLDERSADDRESSLINE2 VARCHAR,
    ZACCOUNTHOLDERSADDRESSSTATE VARCHAR,
    ZACCOUNTHOLDERSADDRESSZIPCODE VARCHAR,
    ZACCOUNTHOLDERSNAME VARCHAR,
    ZACCOUNTNUMBER VARCHAR,
    ZACCOUNTNUMBERSUFFIX VARCHAR,
    ZBANKTYPE VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZNAME VARCHAR,
    ZOWNERTYPE VARCHAR,
    ZROUTINGNUMBER VARCHAR,
    ZSYNCID VARCHAR,
    ZTYPE VARCHAR,
    ZVERIFICATIONSTATUS VARCHAR,
    ZOWNERTYPECONFIRMEDBYUSER INTEGER,
    ZAUTOVERIFIEDATTEMPTED INTEGER,
    ZACCEPTEDACHTERMS INTEGER
)
```

#### ZBILLPRESENTMENTACCOUNT

```sql
CREATE TABLE ZBILLPRESENTMENTACCOUNT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZAGGPROVIDERACCOUNT INTEGER,
    ZUSERPAYEE INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZLASTAPPLIEDBILLDUEDATE TIMESTAMP,
    ZLASTSEENBILLDUEDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZBILLERID VARCHAR,
    ZBILLERLOGOKEY VARCHAR,
    ZBILLERNAME VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZBPFINANCIALINSTITUTION

```sql
CREATE TABLE ZBPFINANCIALINSTITUTION (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZBILLPAYFILEERRORRECOVERYSUPPORTED INTEGER,
    ZBILLPAYSUPPORTED INTEGER,
    ZCANADDPAYEE INTEGER,
    ZCANSCHEDULETRANSFER INTEGER,
    ZDEFAULTPAYMENTDAYSTOPAY INTEGER,
    ZDEFAULTPAYMENTWITHDRAWALDAYS INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZMODELWINDOW INTEGER,
    ZPAYMENTSBYBILLINGADDRESS INTEGER,
    ZPAYMENTSBYPAYEEID INTEGER,
    ZPOSTPROCESSINGWINDOW INTEGER,
    ZQUICKENID INTEGER,
    ZSTATUSVIAMODIFYRESPONSE INTEGER,
    ZTRANSFERFILEERRORRECOVERYSUPPORTED INTEGER,
    ZTRANSFERSUPPORTED INTEGER,
    ZFINANCIALINSTITUTION INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZBILLPAYSYNCMODESUPPORTED VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZINTUDATEMODEL VARCHAR,
    ZPROCESSINGDAYSOFF VARCHAR,
    ZPROCESSINGENDTIME VARCHAR,
    ZSYNCID VARCHAR,
    ZTRANSFERSYNCMODESUPPORTED VARCHAR
)
```

#### ZBPFIPAYEE

```sql
CREATE TABLE ZBPFIPAYEE (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDAYSTOPAY INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZPAYEEINFOCHANGED INTEGER,
    ZPAYEEINFODELETE INTEGER,
    ZQUICKENID INTEGER,
    ZUSERPAYEE INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZADDRESS1 VARCHAR,
    ZADDRESS2 VARCHAR,
    ZADDRESS3 VARCHAR,
    ZCITY VARCHAR,
    ZCLIENTASSIGNEDTRANSACTIONUID VARCHAR,
    ZCOUNTRY VARCHAR,
    ZCUSTOMERID VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZONLINEBILLPAYBID VARCHAR,
    ZPAYEEIDSCOPE VARCHAR,
    ZPAYEELISTID VARCHAR,
    ZPHONENUMBER VARCHAR,
    ZPOSTALCODE VARCHAR,
    ZSTANDARDPAYEEID VARCHAR,
    ZSTANDARDPAYEENAME VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZCHECKPAYCHECK

```sql
CREATE TABLE ZCHECKPAYCHECK (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZBILLPAYFUNDINGACCOUNT INTEGER,
    ZCHECKPAYPAYEE INTEGER,
    ZTRANSACTION INTEGER,
    Z80_TRANSACTION INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZPROCESSINGDATE TIMESTAMP,
    ZSCHEDULEDMAILDATE TIMESTAMP,
    ZSTATUSMODIFIEDTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZCHECKNUMBER VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZMODE VARCHAR,
    ZPRINTEDMEMO VARCHAR,
    ZSTATUS VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZCHECKPAYPAYEE

```sql
CREATE TABLE ZCHECKPAYPAYEE (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZCHECK INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZACCOUNTNUMBER VARCHAR,
    ZADDRESSLINE1 VARCHAR,
    ZADDRESSLINE2 VARCHAR,
    ZCITY VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZNAME VARCHAR,
    ZSTATE VARCHAR,
    ZSYNCID VARCHAR,
    ZZIPCODE VARCHAR
)
```

#### ZIFSBILLPAYBILLERACCOUNT

```sql
CREATE TABLE ZIFSBILLPAYBILLERACCOUNT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZUSERPAYEE INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZACCOUNTHOLDERSNAME VARCHAR,
    ZACCOUNTNUMBER VARCHAR,
    ZBILLERID VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZNICKNAME VARCHAR,
    ZSYNCID VARCHAR,
    ZACCOUNTNUMBERSUFFIX VARCHAR,
    ZBILLERNAME VARCHAR
)
```

#### ZIFSBILLPAYPAYMENT

```sql
CREATE TABLE ZIFSBILLPAYPAYMENT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZARCHIVED INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZFUNDINGACCOUNT INTEGER,
    ZIFSBILLERACCOUNT INTEGER,
    ZTRANSACTION INTEGER,
    Z80_TRANSACTION INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZEXECUTIONCOMPLETEDONTIMESTAMP TIMESTAMP,
    ZEXECUTIONSTARTEDONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZPROPOSEDARRIVEONDATE TIMESTAMP,
    ZPROPOSEDINITIATEONDATE TIMESTAMP,
    ZSCHEDULECOMPLETEDONTIMESTAMP TIMESTAMP,
    ZSCHEDULESTARTEDONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSTATUS VARCHAR,
    ZSYNCID VARCHAR,
    ZTIMEZONEIDENTIFIER VARCHAR,
    ZQCSNOTICE VARCHAR,
    ZQCSDETAILSJSONSTRING VARCHAR,
    ZPARTIALRETURN INTEGER
)
```

#### ZIFSPRESENTMENTBILLERACCOUNT

```sql
CREATE TABLE ZIFSPRESENTMENTBILLERACCOUNT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZNEEDSDELETE INTEGER,
    ZNEEDSDISPATCH INTEGER,
    ZQUICKENID INTEGER,
    ZBILLDATE TIMESTAMP,
    ZBILLDUEDATE TIMESTAMP,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZLASTAPPLIEDBILLDUEDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZBILLCURRENTBALANCE DECIMAL,
    ZBILLMINIMUMAMOUNTDUE DECIMAL,
    ZBILLTOTALAMOUNTDUE DECIMAL,
    ZADDITIONALINFODETAILSJSON VARCHAR,
    ZBILLERACTIVATIONSTRING VARCHAR,
    ZBILLERCLASSIFICATIONSTRING VARCHAR,
    ZBILLERID VARCHAR,
    ZBILLERLOGOKEY VARCHAR,
    ZBILLERMODESTRING VARCHAR,
    ZBILLERNAME VARCHAR,
    ZBILLERNETWORKSTRING VARCHAR,
    ZBILLERURL VARCHAR,
    ZDATASOURCE VARCHAR,
    ZDEACTIVATEDBYSTRING VARCHAR,
    ZGUID VARCHAR,
    ZSTATUSCODE VARCHAR,
    ZSTATUSDESCRIPTION VARCHAR,
    ZSTATUSSTRING VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZQUICKPAYPAYMENT

```sql
CREATE TABLE ZQUICKPAYPAYMENT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZBILLPAYFUNDINGACCOUNT INTEGER,
    ZTRANSACTION INTEGER,
    Z80_TRANSACTION INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZPAYMENTTIMESTAMP TIMESTAMP,
    ZSCHEDULEDPAYMENTDATE TIMESTAMP,
    ZAMOUNTPAID DECIMAL,
    ZFEESPAID DECIMAL,
    ZTOTALPAID DECIMAL,
    ZCONFIRMATIONNUMBER VARCHAR,
    ZDATASOURCE VARCHAR,
    ZEXTENDEDSTATUS VARCHAR,
    ZEXTENDEDSTATUSMESSAGE VARCHAR,
    ZGUID VARCHAR,
    ZMODE VARCHAR,
    ZSTATUS VARCHAR,
    ZSYNCID VARCHAR
)
```

### Budgets & Reports

#### ZBUDGET

```sql
CREATE TABLE ZBUDGET (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZSHOWCENTS INTEGER,
    ZSTARTMONTH INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZCURRENCY VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZNAME VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZBUDGETLINEITEM

```sql
CREATE TABLE ZBUDGETLINEITEM (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZROLLOVER INTEGER,
    ZBUDGET INTEGER,
    ZCATEGORYTAG INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR,
    ZACCOUNT INTEGER,
    ZISTRANSFEROUT INTEGER,
    ZTYPE INTEGER
)
```

#### ZBUDGETTARGET

```sql
CREATE TABLE ZBUDGETTARGET (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZEFFECTIVEDATENUM INTEGER,
    ZQUICKENID INTEGER,
    ZLINEITEM INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZROLLOVERRESETAMT DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZREPORT

```sql
CREATE TABLE ZREPORT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZISPERMANENT INTEGER,
    ZISTEMPLATE INTEGER,
    ZQUICKENID INTEGER,
    ZUSEDINSOURCELIST INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZBASENAME VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZNAME VARCHAR,
    ZSYNCID VARCHAR,
    ZFILTERPARAMETERS BLOB,
    ZPARAMETERS BLOB,
    ZPRINTINFOARCHIVE BLOB,
    ZISTITLEUSERCUSTOMIZED INTEGER,
    ZPARAMETERSJSON VARCHAR,
    ZISTEMPORARY INTEGER,
    ZFILTERPARAMETERSJSON VARCHAR,
    ZOPENEDCOUNT INTEGER,
    ZINTERNALNAME VARCHAR,
    ZEDITEDTIMESTAMP TIMESTAMP,
    ZOPENEDTIMESTAMP TIMESTAMP,
    ZFOLDER INTEGER
)
```

#### ZREPORTFOLDER

```sql
CREATE TABLE ZREPORTFOLDER (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZNAME VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZSMARTREGISTER

```sql
CREATE TABLE ZSMARTREGISTER (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZISPERMANENT INTEGER,
    ZQUICKENID INTEGER,
    ZUSEDINSOURCELIST INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZBASEBEHAVIORCLASSNAME VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZLOCALIZABLENAMEKEY VARCHAR,
    ZNAME VARCHAR,
    ZSYNCID VARCHAR,
    ZUID VARCHAR,
    ZFILTERS BLOB,
    ZFILTERSJSON VARCHAR,
    ZSPENDINGFILTERS BLOB,
    ZINCOMEFILTERS BLOB,
    ZBALANCECHARTFILTERS BLOB
)
```

### Business & Invoicing

#### ZBUSINESS

```sql
CREATE TABLE ZBUSINESS (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZCLOSED INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZISDEFAULT INTEGER,
    ZQUICKENID INTEGER,
    ZINVOICEBACKGROUNDATTACHMENT INTEGER,
    ZLOGOATTACHMENT INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZADDRESS1 VARCHAR,
    ZADDRESS2 VARCHAR,
    ZCITY VARCHAR,
    ZCOLORSTRING VARCHAR,
    ZCOUNTRY VARCHAR,
    ZDATASOURCE VARCHAR,
    ZEMAILADDRESS VARCHAR,
    ZGUID VARCHAR,
    ZIMPORTSOURCEFILEID VARCHAR,
    ZINDUSTRY VARCHAR,
    ZNAME VARCHAR,
    ZNOTES VARCHAR,
    ZPHONENUMBER VARCHAR,
    ZPOSTALCODE VARCHAR,
    ZSTATE VARCHAR,
    ZSYNCID VARCHAR,
    ZTYPESTRING VARCHAR,
    ZWEBSITE VARCHAR,
    ZUSERMODIFIEDTIMESTAMP TIMESTAMP,
    ZDEFAULTPAYMENTTERMS INTEGER
)
```

#### ZCUSTOMER

```sql
CREATE TABLE ZCUSTOMER (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZHIDDEN INTEGER,
    ZQUICKENID INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZADDRESS1 VARCHAR,
    ZADDRESS2 VARCHAR,
    ZCITY VARCHAR,
    ZCOMPANYNAME VARCHAR,
    ZCOUNTRY VARCHAR,
    ZCUSTOMNAME VARCHAR,
    ZDATASOURCE VARCHAR,
    ZEMAILADDRESS VARCHAR,
    ZFIRSTNAME VARCHAR,
    ZGUID VARCHAR,
    ZLASTNAME VARCHAR,
    ZNAME VARCHAR,
    ZNOTES VARCHAR,
    ZPHONENUMBER VARCHAR,
    ZPOSTALCODE VARCHAR,
    ZSTATE VARCHAR,
    ZSYNCID VARCHAR,
    ZWEBSITE VARCHAR,
    ZUSERMODIFIEDTIMESTAMP TIMESTAMP
)
```

#### ZINVOICE

```sql
CREATE TABLE ZINVOICE (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZACCOUNTINGRECORD INTEGER,
    ZBUSINESS INTEGER,
    ZCUSTOMER INTEGER,
    ZINVOICEBACKGROUNDATTACHMENT INTEGER,
    ZLOGOATTACHMENT INTEGER,
    ZPROJECT INTEGER,
    ZSALESTAXRATE INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZDUEDATE TIMESTAMP,
    ZINVOICEDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZVOIDDATE TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZDISCOUNTVALUE DECIMAL,
    ZFROZEN_TAXRATE_RATE DECIMAL,
    ZSALESTAXAMOUNT DECIMAL,
    ZVOIDEDAMOUNT DECIMAL,
    ZCURRENCY VARCHAR,
    ZDATASOURCE VARCHAR,
    ZDISCOUNTTYPESTRING VARCHAR,
    ZFROZEN_BUSINESS_ADDRESS1 VARCHAR,
    ZFROZEN_BUSINESS_ADDRESS2 VARCHAR,
    ZFROZEN_BUSINESS_CITY VARCHAR,
    ZFROZEN_BUSINESS_COUNTRY VARCHAR,
    ZFROZEN_BUSINESS_EMAILADDRESS VARCHAR,
    ZFROZEN_BUSINESS_NAME VARCHAR,
    ZFROZEN_BUSINESS_PHONENUMBER VARCHAR,
    ZFROZEN_BUSINESS_POSTALCODE VARCHAR,
    ZFROZEN_BUSINESS_STATE VARCHAR,
    ZFROZEN_CUSTOMER_ADDRESS1 VARCHAR,
    ZFROZEN_CUSTOMER_ADDRESS2 VARCHAR,
    ZFROZEN_CUSTOMER_CITY VARCHAR,
    ZFROZEN_CUSTOMER_COMPANYNAME VARCHAR,
    ZFROZEN_CUSTOMER_COUNTRY VARCHAR,
    ZFROZEN_CUSTOMER_EMAILADDRESS VARCHAR,
    ZFROZEN_CUSTOMER_FIRSTNAME VARCHAR,
    ZFROZEN_CUSTOMER_LASTNAME VARCHAR,
    ZFROZEN_CUSTOMER_PHONENUMBER VARCHAR,
    ZFROZEN_CUSTOMER_POSTALCODE VARCHAR,
    ZFROZEN_CUSTOMER_STATE VARCHAR,
    ZFROZEN_PROJECT_NAME VARCHAR,
    ZFROZEN_TAXRATE_NAME VARCHAR,
    ZGUID VARCHAR,
    ZINVOICENUMBERSTRING VARCHAR,
    ZNOTES VARCHAR,
    ZSTATUSSTRING VARCHAR,
    ZSYNCID VARCHAR,
    ZFROZEN_BUSINESS_WEBSITE VARCHAR,
    ZEMAILSUBJECT VARCHAR,
    ZEMAILMESSAGE VARCHAR,
    ZPAYMENTTERMS INTEGER
)
```

#### ZINVOICELINEITEM

```sql
CREATE TABLE ZINVOICELINEITEM (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZSEQUENCENUMBER INTEGER,
    ZTAXABLE INTEGER,
    ZBILLEDTRANSACTIONENTRY INTEGER,
    ZINVOICE INTEGER,
    ZMILEAGEENTRY INTEGER,
    ZPRODUCTSERVICE INTEGER,
    ZTRANSACTIONENTRY INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZUNITPRICEFORVOIDEDINVOICE DECIMAL,
    ZUNITS DECIMAL,
    ZDATASOURCE VARCHAR,
    ZFROZEN_PRODUCTSERVICE_NAME VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR,
    ZUSERDESCRIPTION VARCHAR
)
```

#### ZCUSTOMERCREDIT

```sql
CREATE TABLE ZCUSTOMERCREDIT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZACCOUNTINGRECORD INTEGER,
    ZBUSINESS INTEGER,
    ZCUSTOMER INTEGER,
    ZINVOICE INTEGER,
    ZSALESTAXRATE INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZDISCOUNTAMOUNT DECIMAL,
    ZREMAINDERAMOUNT DECIMAL,
    ZSALESTAXAMOUNT DECIMAL,
    ZCURRENCY VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZNOTES VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZCUSTOMERCREDITALLOCATION

```sql
CREATE TABLE ZCUSTOMERCREDITALLOCATION (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZCUSTOMERCREDIT INTEGER,
    ZCUSTOMERREFUND INTEGER,
    ZINVOICE INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZCUSTOMERCREDITLINEITEM

```sql
CREATE TABLE ZCUSTOMERCREDITLINEITEM (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZSEQUENCENUMBER INTEGER,
    ZTAXABLE INTEGER,
    ZCATEGORY INTEGER,
    ZCUSTOMERCREDIT INTEGER,
    ZPRODUCTSERVICE INTEGER,
    ZTRANSACTIONENTRY INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZUNITS DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR,
    ZUSERDESCRIPTION VARCHAR
)
```

#### ZCUSTOMERPAYMENT

```sql
CREATE TABLE ZCUSTOMERPAYMENT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZACCOUNTINGRECORD INTEGER,
    ZBUSINESS INTEGER,
    ZCUSTOMER INTEGER,
    ZTRANSACTIONENTRY INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZREMAINDERAMOUNT DECIMAL,
    ZCURRENCY VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZCUSTOMERPAYMENTALLOCATION

```sql
CREATE TABLE ZCUSTOMERPAYMENTALLOCATION (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZCUSTOMERPAYMENT INTEGER,
    ZINVOICE INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR,
    ZCUSTOMERREFUND INTEGER
)
```

#### ZCUSTOMERREFUND

```sql
CREATE TABLE ZCUSTOMERREFUND (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZACCOUNTINGRECORD INTEGER,
    ZBUSINESS INTEGER,
    ZCUSTOMER INTEGER,
    ZTRANSACTIONENTRY INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZCURRENCY VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZSALESTAXAGENCY

```sql
CREATE TABLE ZSALESTAXAGENCY (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZHIDDEN INTEGER,
    ZQUICKENID INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZDATASOURCE VARCHAR,
    ZEMAIL VARCHAR,
    ZFILINGFREQUENCY VARCHAR,
    ZGUID VARCHAR,
    ZNAME VARCHAR,
    ZPHONENUMBER VARCHAR,
    ZSYNCID VARCHAR,
    ZWEBSITE VARCHAR
)
```

#### ZSALESTAXPAYMENT

```sql
CREATE TABLE ZSALESTAXPAYMENT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZACCOUNTINGRECORD INTEGER,
    ZBUSINESS INTEGER,
    ZSALESTAXAGENCY INTEGER,
    ZTRANSACTIONENTRY INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZCURRENCY VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZSALESTAXRATE

```sql
CREATE TABLE ZSALESTAXRATE (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZHIDDEN INTEGER,
    ZQUICKENID INTEGER,
    ZSALESTAXAGENCY INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZRATE DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZNAME VARCHAR,
    ZSYNCID VARCHAR,
    ZTAXAGENCYNAME VARCHAR,
    ZUSERDESCRIPTION VARCHAR,
    ZUSERMODIFIEDTIMESTAMP TIMESTAMP
)
```

#### ZPRODUCTSERVICE

```sql
CREATE TABLE ZPRODUCTSERVICE (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZHIDDEN INTEGER,
    ZQUICKENID INTEGER,
    ZTAXABLE INTEGER,
    ZCATEGORY INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZUNITPRICE DECIMAL,
    ZCURRENCY VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZNAME VARCHAR,
    ZSYNCID VARCHAR,
    ZUSERDESCRIPTION VARCHAR,
    ZUSERMODIFIEDTIMESTAMP TIMESTAMP
)
```

#### ZPROJECT

```sql
CREATE TABLE ZPROJECT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZCUSTOMER INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZNAME VARCHAR,
    ZNOTES VARCHAR,
    ZSTATUSSTRING VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZMILEAGEENTRY

```sql
CREATE TABLE ZMILEAGEENTRY (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZBUSINESS INTEGER,
    ZROUNDTRIPENTRY INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZENTEREDDATE TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZDISTANCEMILES DECIMAL,
    ZDATASOURCE VARCHAR,
    ZENDINGPOINT VARCHAR,
    ZGUID VARCHAR,
    ZNOTES VARCHAR,
    ZPURPOSE VARCHAR,
    ZSTARTINGPOINT VARCHAR,
    ZSYNCID VARCHAR,
    ZTRIPTYPE VARCHAR,
    ZBILLABLE INTEGER,
    ZCUSTOMER INTEGER,
    ZINVOICELINEITEM INTEGER
)
```

#### ZTAXPAYMENTRECORD

```sql
CREATE TABLE ZTAXPAYMENTRECORD (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUARTER INTEGER,
    ZQUICKENID INTEGER,
    ZYEAR INTEGER,
    ZBUSINESS INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

### Configuration & Metadata

#### ZDOCUMENTPROPERTY

```sql
CREATE TABLE ZDOCUMENTPROPERTY (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZINTVALUE INTEGER,
    ZTYPE INTEGER,
    ZDATEVALUE TIMESTAMP,
    ZNAME VARCHAR,
    ZSTRINGVALUE VARCHAR,
    ZBINARYVALUE BLOB
)
```

#### ZQUICKFILLRULE

```sql
CREATE TABLE ZQUICKFILLRULE (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZISNOTEDITABLEBYTRANSACTIONS INTEGER,
    ZMARKASCLEARED INTEGER,
    ZNEVERAUTOCATEGORIZE INTEGER,
    ZQUICKENID INTEGER,
    ZUSEFORDOWNLOADEDTRANSACTIONS INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZLASTUSEDTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZMEMO VARCHAR,
    ZPAYEENAME VARCHAR,
    ZSYNCID VARCHAR,
    ZTRANSACTIONTYPE VARCHAR,
    ZUSERMODIFIEDTIMESTAMP TIMESTAMP
)
```

#### ZQUICKFILLRULESPLITENTRY

```sql
CREATE TABLE ZQUICKFILLRULESPLITENTRY (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZSEQUENCENUMBER INTEGER,
    ZCATEGORYTAG INTEGER,
    ZQUICKFILLRULE INTEGER,
    ZTRANSFERACCOUNT INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZAMOUNT DECIMAL,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZMEMO VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZRENAMINGRULE

```sql
CREATE TABLE ZRENAMINGRULE (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZDONTRENAMEPAYEE INTEGER,
    ZQUICKENID INTEGER,
    ZRULETYPE INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZRULELASTUSEDDATE TIMESTAMP,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZORIGINALQUICKENNAME VARCHAR,
    ZORIGINALSTATEMENTNAME VARCHAR,
    ZRULEPATTERN VARCHAR,
    ZSYNCID VARCHAR,
    ZTARGETPAYEENAME VARCHAR,
    ZUSERMODIFIEDTIMESTAMP TIMESTAMP
)
```

#### ZALERT

```sql
CREATE TABLE ZALERT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZISREAD INTEGER,
    ZQUICKENID INTEGER,
    ZCREATEDAT TIMESTAMP,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZDATASOURCE VARCHAR,
    ZGROUP VARCHAR,
    ZGUID VARCHAR,
    ZICON VARCHAR,
    ZMESSAGE VARCHAR,
    ZSYNCID VARCHAR,
    ZTITLE VARCHAR,
    ZTYPE VARCHAR
)
```

#### ZATTACHMENT

```sql
CREATE TABLE ZATTACHMENT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZSYNCWITHCLOUD INTEGER,
    ZACCOUNT INTEGER,
    ZBUSINESS INTEGER,
    ZCUSTOMER INTEGER,
    ZPROJECT INTEGER,
    ZTRANSACTION INTEGER,
    Z79_TRANSACTION INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZCLOUDURI VARCHAR,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZNOTES VARCHAR,
    ZORIGINALFILEPATH VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZQERROR

```sql
CREATE TABLE ZQERROR (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZERRORCODE INTEGER,
    ZERRORSTATUS INTEGER,
    ZQUICKENID INTEGER,
    ZSEVERITYLEVEL INTEGER,
    ZACCOUNT INTEGER,
    ZFINANCIALINSTITUTION INTEGER,
    ZTRANSACTION INTEGER,
    Z79_TRANSACTION INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZDATASOURCE VARCHAR,
    ZERRORDESCRIPTION VARCHAR,
    ZERRORDOMAIN VARCHAR,
    ZGUID VARCHAR,
    ZRECOVERYSUGGESTION VARCHAR,
    ZSUBCLASSNAME VARCHAR,
    ZSYNCID VARCHAR,
    ZCUSTOMINFORMATION BLOB,
    ZCUSTOMINFORMATIONJSON VARCHAR
)
```

#### ZQOBJECT

```sql
CREATE TABLE ZQOBJECT (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZQUICKENID INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZDATASOURCE VARCHAR,
    ZGUID VARCHAR,
    ZSYNCID VARCHAR
)
```

#### ZSYNCERROR

```sql
CREATE TABLE ZSYNCERROR (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZDELETIONCOUNT INTEGER,
    ZERRORLEVEL INTEGER,
    ZERRORSTATE INTEGER,
    ZQUICKENID INTEGER,
    ZRESOLVEOPTIONSELECTED INTEGER,
    ZCREATIONTIMESTAMP TIMESTAMP,
    ZMODIFICATIONTIMESTAMP TIMESTAMP,
    ZDATASOURCE VARCHAR,
    ZERRORSPECIFICDATA VARCHAR,
    ZGUID VARCHAR,
    ZINSTANCECLASSNAME VARCHAR,
    ZSYNCID VARCHAR,
    ZSYSTEMDESCRIPTION VARCHAR,
    ZUSERDESCRIPTION VARCHAR
)
```

#### ZCLOUDSYNCDATA

```sql
CREATE TABLE ZCLOUDSYNCDATA (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZQUICKENTIMESTAMP TIMESTAMP,
    ZMINTWATERMARK VARCHAR,
    ZRESOURCETYPE VARCHAR
)
```
#### ZTAXLINEITEM

```sql
CREATE TABLE ZTAXLINEITEM (
    Z_PK INTEGER PRIMARY KEY,
    Z_ENT INTEGER,
    Z_OPT INTEGER,
    ZFORM VARCHAR,
    ZLINENUMBER VARCHAR,
    ZUSERDESCRIPTION VARCHAR
)
```


### Join Tables (Many-to-Many)

#### Z_15USERTAGS

```sql
CREATE TABLE "Z_15USERTAGS" (
    Z_15CASHFLOWTRANSACTIONENTRIES INTEGER,
    Z_76USERTAGS INTEGER,
    PRIMARY KEY (Z_15CASHFLOWTRANSACTIONENTRIES, Z_76USERTAGS)
)
```

#### Z_55USERTAGS

```sql
CREATE TABLE "Z_55USERTAGS" (
    Z_55QUICKFILLRULESPLITENTRIES INTEGER,
    Z_76USERTAGS1 INTEGER,
    PRIMARY KEY (Z_55QUICKFILLRULESPLITENTRIES, Z_76USERTAGS1)
)
```

#### Z_1BUDGETS

```sql
CREATE TABLE Z_1BUDGETS (
    Z_1ACCOUNTS INTEGER,
    Z_9BUDGETS INTEGER,
    PRIMARY KEY (Z_1ACCOUNTS, Z_9BUDGETS)
)
```

#### Z_1DOWNLOADSESSIONS

```sql
CREATE TABLE Z_1DOWNLOADSESSIONS (
    Z_1ACCOUNTS1 INTEGER,
    Z_30DOWNLOADSESSIONS INTEGER,
    PRIMARY KEY (Z_1ACCOUNTS1, Z_30DOWNLOADSESSIONS)
)
```

### Core Data System Tables

#### Z_PRIMARYKEY

```sql
CREATE TABLE Z_PRIMARYKEY (
    Z_ENT INTEGER PRIMARY KEY,
    Z_NAME VARCHAR,
    Z_SUPER INTEGER,
    Z_MAX INTEGER
)
```

#### Z_METADATA

```sql
CREATE TABLE Z_METADATA (
    Z_VERSION INTEGER PRIMARY KEY,
    Z_UUID VARCHAR(255),
    Z_PLIST BLOB
)
```

#### Z_MODELCACHE

```sql
CREATE TABLE Z_MODELCACHE (
    Z_CONTENT BLOB
)
```

---

## Join Tables (Many-to-Many Relationships)

Core Data uses numbered join tables for many-to-many relationships. The numbers in table and column names correspond to `Z_ENT` values and **vary per database**.

### Z_15USERTAGS

Links `CashFlowTransactionEntry` rows to `UserTag` rows (user-defined tags on transaction splits).

```sql
CREATE TABLE "Z_15USERTAGS" (
    Z_15CASHFLOWTRANSACTIONENTRIES INTEGER,
    Z_76USERTAGS INTEGER,
    PRIMARY KEY (Z_15CASHFLOWTRANSACTIONENTRIES, Z_76USERTAGS)
)
```

Column name mapping (for this particular database snapshot):
- `Z_15CASHFLOWTRANSACTIONENTRIES` -- FK to `ZCASHFLOWTRANSACTIONENTRY.Z_PK`
- `Z_76USERTAGS` -- FK to `ZTAG.Z_PK` where `Z_ENT` = UserTag

### Z_55USERTAGS

Links `QuickFillRuleSplitEntry` rows to `UserTag` rows (tags on auto-fill rules).

```sql
CREATE TABLE "Z_55USERTAGS" (
    Z_55QUICKFILLRULESPLITENTRIES INTEGER,
    Z_76USERTAGS1 INTEGER,
    PRIMARY KEY (Z_55QUICKFILLRULESPLITENTRIES, Z_76USERTAGS1)
)
```

Column name mapping:
- `Z_55QUICKFILLRULESPLITENTRIES` -- FK to `ZQUICKFILLRULESPLITENTRY.Z_PK`
- `Z_76USERTAGS1` -- FK to `ZTAG.Z_PK` where `Z_ENT` = UserTag

### Z_1BUDGETS

Links `Account` rows to `Budget` rows (which accounts are included in which budgets).

```sql
CREATE TABLE Z_1BUDGETS (
    Z_1ACCOUNTS INTEGER,
    Z_9BUDGETS INTEGER,
    PRIMARY KEY (Z_1ACCOUNTS, Z_9BUDGETS)
)
```

Column name mapping:
- `Z_1ACCOUNTS` -- FK to `ZACCOUNT.Z_PK`
- `Z_9BUDGETS` -- FK to `ZBUDGET.Z_PK`

### Z_1DOWNLOADSESSIONS

Links `Account` rows to `DownloadSession` rows (which accounts participated in a download).

```sql
CREATE TABLE Z_1DOWNLOADSESSIONS (
    Z_1ACCOUNTS1 INTEGER,
    Z_30DOWNLOADSESSIONS INTEGER,
    PRIMARY KEY (Z_1ACCOUNTS1, Z_30DOWNLOADSESSIONS)
)
```

Column name mapping:
- `Z_1ACCOUNTS1` -- FK to `ZACCOUNT.Z_PK`
- `Z_30DOWNLOADSESSIONS` -- FK to `ZDOWNLOADSESSION.Z_PK`

---

## Indexes

All 251 indexes, grouped by table. Core Data creates both FK-based relationship indexes (`ZTABLE_ZCOLUMN_INDEX`) and semantic indexes (`Z_EntityName_propertyName`).


**ZACCOUNT**

- `ZACCOUNT_ZBILLPAYFUNDINGACCOUNT_INDEX` on `ZBILLPAYFUNDINGACCOUNT`
- `ZACCOUNT_ZBILLPAYPROVIDERINSTITUTION_INDEX` on `ZBILLPAYPROVIDERINSTITUTION`
- `ZACCOUNT_ZBPSACCOUNT_INDEX` on `ZBPSACCOUNT`
- `ZACCOUNT_ZCLOUDFILOGIN_INDEX` on `ZCLOUDFILOGIN`
- `ZACCOUNT_ZERROR_INDEX` on `ZERROR`
- `ZACCOUNT_ZFINANCIALINSTITUTION_INDEX` on `ZFINANCIALINSTITUTION`
- `ZACCOUNT_ZLOANINTERESTCATEGORY_INDEX` on `ZLOANINTERESTCATEGORY`
- `ZACCOUNT_ZLOANSMARTTRANSACTION_INDEX` on `ZLOANSMARTTRANSACTION`
- `ZACCOUNT_ZPRIMARYUSEBUSINESS_INDEX` on `ZPRIMARYUSEBUSINESS`

**ZACCOUNTINGRECORD**

- `ZACCOUNTINGRECORD_ZBUSINESS_INDEX` on `ZBUSINESS`
- `ZACCOUNTINGRECORD_ZCUSTOMERCREDIT_INDEX` on `ZCUSTOMERCREDIT`
- `ZACCOUNTINGRECORD_ZCUSTOMERPAYMENT_INDEX` on `ZCUSTOMERPAYMENT`
- `ZACCOUNTINGRECORD_ZCUSTOMERREFUND_INDEX` on `ZCUSTOMERREFUND`
- `ZACCOUNTINGRECORD_ZINVOICE_INDEX` on `ZINVOICE`
- `ZACCOUNTINGRECORD_ZSALESTAXPAYMENT_INDEX` on `ZSALESTAXPAYMENT`

**ZATTACHMENT**

- `ZATTACHMENT_ZACCOUNT_INDEX` on `ZACCOUNT`
- `ZATTACHMENT_ZBUSINESS_INDEX` on `ZBUSINESS`
- `ZATTACHMENT_ZCUSTOMER_INDEX` on `ZCUSTOMER`
- `ZATTACHMENT_ZPROJECT_INDEX` on `ZPROJECT`
- `ZATTACHMENT_ZTRANSACTION_INDEX` on `ZTRANSACTION`

**ZBILLPAYFUNDINGACCOUNT**

- `ZBILLPAYFUNDINGACCOUNT_ZACCOUNT_INDEX` on `ZACCOUNT`

**ZBILLPRESENTMENTACCOUNT**

- `ZBILLPRESENTMENTACCOUNT_ZAGGPROVIDERACCOUNT_INDEX` on `ZAGGPROVIDERACCOUNT`
- `ZBILLPRESENTMENTACCOUNT_ZUSERPAYEE_INDEX` on `ZUSERPAYEE`

**ZBPFINANCIALINSTITUTION**

- `ZBPFINANCIALINSTITUTION_ZFINANCIALINSTITUTION_INDEX` on `ZFINANCIALINSTITUTION`

**ZBPFIPAYEE**

- `ZBPFIPAYEE_ZUSERPAYEE_INDEX` on `ZUSERPAYEE`

**ZBUDGETLINEITEM**

- `ZBUDGETLINEITEM_ZACCOUNT_INDEX` on `ZACCOUNT`
- `ZBUDGETLINEITEM_ZBUDGET_INDEX` on `ZBUDGET`
- `ZBUDGETLINEITEM_ZCATEGORYTAG_INDEX` on `ZCATEGORYTAG`
- `Z_BudgetLineItem_syncID` on `ZSYNCID`

**ZBUDGETTARGET**

- `ZBUDGETTARGET_ZLINEITEM_INDEX` on `ZLINEITEM`
- `Z_BudgetTarget_effectiveDateNum` on `ZEFFECTIVEDATENUM`
- `Z_BudgetTarget_syncID` on `ZSYNCID`

**ZBUSINESS**

- `ZBUSINESS_ZINVOICEBACKGROUNDATTACHMENT_INDEX` on `ZINVOICEBACKGROUNDATTACHMENT`
- `ZBUSINESS_ZLOGOATTACHMENT_INDEX` on `ZLOGOATTACHMENT`

**ZCASHFLOWTRANSACTIONENTRY**

- `ZCASHFLOWTRANSACTIONENTRY_ZBILLEDINVOICELINEITEM_INDEX` on `ZBILLEDINVOICELINEITEM`
- `ZCASHFLOWTRANSACTIONENTRY_ZBUSINESS_INDEX` on `ZBUSINESS`
- `ZCASHFLOWTRANSACTIONENTRY_ZCATEGORYTAG_INDEX` on `ZCATEGORYTAG`
- `ZCASHFLOWTRANSACTIONENTRY_ZCUSTOMERCREDITLINEITEM_INDEX` on `ZCUSTOMERCREDITLINEITEM`
- `ZCASHFLOWTRANSACTIONENTRY_ZCUSTOMERPAYMENT_INDEX` on `ZCUSTOMERPAYMENT`
- `ZCASHFLOWTRANSACTIONENTRY_ZCUSTOMERREFUND_INDEX` on `ZCUSTOMERREFUND`
- `ZCASHFLOWTRANSACTIONENTRY_ZCUSTOMER_INDEX` on `ZCUSTOMER`
- `ZCASHFLOWTRANSACTIONENTRY_ZINVOICELINEITEM_INDEX` on `ZINVOICELINEITEM`
- `ZCASHFLOWTRANSACTIONENTRY_ZPARENT_INDEX` on `ZPARENT`
- `ZCASHFLOWTRANSACTIONENTRY_ZPROJECT_INDEX` on `ZPROJECT`
- `ZCASHFLOWTRANSACTIONENTRY_ZSALESTAXPAYMENT_INDEX` on `ZSALESTAXPAYMENT`
- `Z_CashFlowTransactionEntry_quickenId` on `ZQUICKENID`
- `Z_CashFlowTransactionEntry_syncID` on `ZSYNCID`

**ZCHECKPAYCHECK**

- `ZCHECKPAYCHECK_ZBILLPAYFUNDINGACCOUNT_INDEX` on `ZBILLPAYFUNDINGACCOUNT`
- `ZCHECKPAYCHECK_ZCHECKPAYPAYEE_INDEX` on `ZCHECKPAYPAYEE`
- `ZCHECKPAYCHECK_ZTRANSACTION_INDEX` on `ZTRANSACTION`

**ZCHECKPAYPAYEE**

- `ZCHECKPAYPAYEE_ZCHECK_INDEX` on `ZCHECK`

**ZCLOUDFILOGIN**

- `ZCLOUDFILOGIN_ZFINANCIALINSTITUTION_INDEX` on `ZFINANCIALINSTITUTION`

**ZCONNECTIVITYEVENTRESULT**

- `ZCONNECTIVITYEVENTRESULT_ZACCOUNT_INDEX` on `ZACCOUNT`
- `ZCONNECTIVITYEVENTRESULT_ZCLOUDFILOGIN_INDEX` on `ZCLOUDFILOGIN`
- `ZCONNECTIVITYEVENTRESULT_ZDOWNLOADSESSION_INDEX` on `ZDOWNLOADSESSION`
- `ZCONNECTIVITYEVENTRESULT_ZSHADOWACCOUNT_INDEX` on `ZSHADOWACCOUNT`
- `ZCONNECTIVITYEVENTRESULT_ZTRANSACTION_INDEX` on `ZTRANSACTION`
- `Z_ConnectivityEventResult_byLastSeenTimestampIndex` on `(ZACCOUNT, ZLASTSEENTIMESTAMP)`
- `Z_ConnectivityEventResult_guid` on `ZGUID`
- `Z_ConnectivityEventResult_quickenId` on `ZQUICKENID`

**ZCUSTOMERCREDIT**

- `ZCUSTOMERCREDIT_ZACCOUNTINGRECORD_INDEX` on `ZACCOUNTINGRECORD`
- `ZCUSTOMERCREDIT_ZBUSINESS_INDEX` on `ZBUSINESS`
- `ZCUSTOMERCREDIT_ZCUSTOMER_INDEX` on `ZCUSTOMER`
- `ZCUSTOMERCREDIT_ZINVOICE_INDEX` on `ZINVOICE`
- `ZCUSTOMERCREDIT_ZSALESTAXRATE_INDEX` on `ZSALESTAXRATE`

**ZCUSTOMERCREDITALLOCATION**

- `ZCUSTOMERCREDITALLOCATION_ZCUSTOMERCREDIT_INDEX` on `ZCUSTOMERCREDIT`
- `ZCUSTOMERCREDITALLOCATION_ZCUSTOMERREFUND_INDEX` on `ZCUSTOMERREFUND`
- `ZCUSTOMERCREDITALLOCATION_ZINVOICE_INDEX` on `ZINVOICE`

**ZCUSTOMERCREDITLINEITEM**

- `ZCUSTOMERCREDITLINEITEM_ZCATEGORY_INDEX` on `ZCATEGORY`
- `ZCUSTOMERCREDITLINEITEM_ZCUSTOMERCREDIT_INDEX` on `ZCUSTOMERCREDIT`
- `ZCUSTOMERCREDITLINEITEM_ZPRODUCTSERVICE_INDEX` on `ZPRODUCTSERVICE`
- `ZCUSTOMERCREDITLINEITEM_ZTRANSACTIONENTRY_INDEX` on `ZTRANSACTIONENTRY`

**ZCUSTOMERPAYMENT**

- `ZCUSTOMERPAYMENT_ZACCOUNTINGRECORD_INDEX` on `ZACCOUNTINGRECORD`
- `ZCUSTOMERPAYMENT_ZBUSINESS_INDEX` on `ZBUSINESS`
- `ZCUSTOMERPAYMENT_ZCUSTOMER_INDEX` on `ZCUSTOMER`
- `ZCUSTOMERPAYMENT_ZTRANSACTIONENTRY_INDEX` on `ZTRANSACTIONENTRY`

**ZCUSTOMERPAYMENTALLOCATION**

- `ZCUSTOMERPAYMENTALLOCATION_ZCUSTOMERPAYMENT_INDEX` on `ZCUSTOMERPAYMENT`
- `ZCUSTOMERPAYMENTALLOCATION_ZCUSTOMERREFUND_INDEX` on `ZCUSTOMERREFUND`
- `ZCUSTOMERPAYMENTALLOCATION_ZINVOICE_INDEX` on `ZINVOICE`

**ZCUSTOMERREFUND**

- `ZCUSTOMERREFUND_ZACCOUNTINGRECORD_INDEX` on `ZACCOUNTINGRECORD`
- `ZCUSTOMERREFUND_ZBUSINESS_INDEX` on `ZBUSINESS`
- `ZCUSTOMERREFUND_ZCUSTOMER_INDEX` on `ZCUSTOMER`
- `ZCUSTOMERREFUND_ZTRANSACTIONENTRY_INDEX` on `ZTRANSACTIONENTRY`

**ZDISPLAYVALUELINE**

- `ZDISPLAYVALUELINE_ZACCOUNT_INDEX` on `ZACCOUNT`

**ZDOCUMENTPROPERTY**

- `ZDOCUMENTPROPERTY_ZNAME_INDEX` on `ZNAME`
- `Z_DocumentProperty_name` on `ZNAME`

**ZDOWNLOADSESSION**

- `ZDOWNLOADSESSION_ZSESSIONNUMBER_INDEX` on `ZSESSIONNUMBER`
- `Z_DownloadSession_sessionNumber` on `ZSESSIONNUMBER`

**ZFIBALANCE**

- `ZFIBALANCE_ZFISTATEMENT_INDEX` on `ZFISTATEMENT`

**ZFINANCIALINSTITUTION**

- `ZFINANCIALINSTITUTION_ZBPFINANCIALINSTITUTION_INDEX` on `ZBPFINANCIALINSTITUTION`

**ZFIPOSITION**

- `ZFIPOSITION_ZFISTATEMENT_INDEX` on `ZFISTATEMENT`
- `ZFIPOSITION_ZUNIQUEID_INDEX` on `ZUNIQUEID`
- `Z_FIPosition_uniqueID` on `ZUNIQUEID`

**ZFISTATEMENT**

- `ZFISTATEMENT_ZACCOUNT_INDEX` on `ZACCOUNT`

**ZFITRANSACTION**

- `ZFITRANSACTION_ZACCOUNT_INDEX` on `ZACCOUNT`
- `Z_FITransaction_fiTransactionID` on `ZFITRANSACTIONID`

**ZIFSBILLPAYBILLERACCOUNT**

- `ZIFSBILLPAYBILLERACCOUNT_ZUSERPAYEE_INDEX` on `ZUSERPAYEE`

**ZIFSBILLPAYPAYMENT**

- `ZIFSBILLPAYPAYMENT_ZFUNDINGACCOUNT_INDEX` on `ZFUNDINGACCOUNT`
- `ZIFSBILLPAYPAYMENT_ZIFSBILLERACCOUNT_INDEX` on `ZIFSBILLERACCOUNT`
- `ZIFSBILLPAYPAYMENT_ZTRANSACTION_INDEX` on `ZTRANSACTION`

**ZINVOICE**

- `ZINVOICE_ZACCOUNTINGRECORD_INDEX` on `ZACCOUNTINGRECORD`
- `ZINVOICE_ZBUSINESS_INDEX` on `ZBUSINESS`
- `ZINVOICE_ZCUSTOMER_INDEX` on `ZCUSTOMER`
- `ZINVOICE_ZINVOICEBACKGROUNDATTACHMENT_INDEX` on `ZINVOICEBACKGROUNDATTACHMENT`
- `ZINVOICE_ZLOGOATTACHMENT_INDEX` on `ZLOGOATTACHMENT`
- `ZINVOICE_ZPROJECT_INDEX` on `ZPROJECT`
- `ZINVOICE_ZSALESTAXRATE_INDEX` on `ZSALESTAXRATE`

**ZINVOICELINEITEM**

- `ZINVOICELINEITEM_ZBILLEDTRANSACTIONENTRY_INDEX` on `ZBILLEDTRANSACTIONENTRY`
- `ZINVOICELINEITEM_ZINVOICE_INDEX` on `ZINVOICE`
- `ZINVOICELINEITEM_ZMILEAGEENTRY_INDEX` on `ZMILEAGEENTRY`
- `ZINVOICELINEITEM_ZPRODUCTSERVICE_INDEX` on `ZPRODUCTSERVICE`
- `ZINVOICELINEITEM_ZTRANSACTIONENTRY_INDEX` on `ZTRANSACTIONENTRY`

**ZLOANSPLITENTRY**

- `ZLOANSPLITENTRY_ZACCOUNT_INDEX` on `ZACCOUNT`
- `ZLOANSPLITENTRY_ZCATEGORY_INDEX` on `ZCATEGORY`

**ZLOT**

- `ZLOT_ZPOSITION_INDEX` on `ZPOSITION`

**ZLOTASSIGNMENT**

- `ZLOTASSIGNMENT_ZDESTINATIONTRANSACTION_INDEX` on `ZDESTINATIONTRANSACTION`
- `ZLOTASSIGNMENT_ZLOTMOD_INDEX` on `ZLOTMOD`
- `ZLOTASSIGNMENT_ZSOURCETRANSACTION_INDEX` on `ZSOURCETRANSACTION`

**ZLOTMOD**

- `ZLOTMOD_ZLOTASSIGNMENT_INDEX` on `ZLOTASSIGNMENT`
- `ZLOTMOD_ZLOT_INDEX` on `ZLOT`
- `ZLOTMOD_ZTRANSACTION_INDEX` on `ZTRANSACTION`

**ZMILEAGEENTRY**

- `ZMILEAGEENTRY_ZBUSINESS_INDEX` on `ZBUSINESS`
- `ZMILEAGEENTRY_ZCUSTOMER_INDEX` on `ZCUSTOMER`
- `ZMILEAGEENTRY_ZINVOICELINEITEM_INDEX` on `ZINVOICELINEITEM`
- `ZMILEAGEENTRY_ZROUNDTRIPENTRY_INDEX` on `ZROUNDTRIPENTRY`

**ZOFXEMAIL**

- `ZOFXEMAIL_ZACCOUNT_INDEX` on `ZACCOUNT`
- `ZOFXEMAIL_ZFISTATEOFX_INDEX` on `ZFISTATEOFX`
- `ZOFXEMAIL_ZTRANSACTION_INDEX` on `ZTRANSACTION`

**ZPOSITION**

- `ZPOSITION_ZACCOUNT_INDEX` on `ZACCOUNT`
- `ZPOSITION_ZSECURITY_INDEX` on `ZSECURITY`
- `ZPOSITION_ZUNIQUEID_INDEX` on `ZUNIQUEID`
- `Z_Position_uniqueID` on `ZUNIQUEID`

**ZPRODUCTSERVICE**

- `ZPRODUCTSERVICE_ZCATEGORY_INDEX` on `ZCATEGORY`

**ZPROJECT**

- `ZPROJECT_ZCUSTOMER_INDEX` on `ZCUSTOMER`

**ZQERROR**

- `ZQERROR_ZACCOUNT_INDEX` on `ZACCOUNT`
- `ZQERROR_ZFINANCIALINSTITUTION_INDEX` on `ZFINANCIALINSTITUTION`
- `ZQERROR_ZTRANSACTION_INDEX` on `ZTRANSACTION`

**ZQUICKFILLRULESPLITENTRY**

- `ZQUICKFILLRULESPLITENTRY_ZCATEGORYTAG_INDEX` on `ZCATEGORYTAG`
- `ZQUICKFILLRULESPLITENTRY_ZQUICKFILLRULE_INDEX` on `ZQUICKFILLRULE`
- `ZQUICKFILLRULESPLITENTRY_ZTRANSFERACCOUNT_INDEX` on `ZTRANSFERACCOUNT`

**ZQUICKPAYPAYMENT**

- `ZQUICKPAYPAYMENT_ZBILLPAYFUNDINGACCOUNT_INDEX` on `ZBILLPAYFUNDINGACCOUNT`
- `ZQUICKPAYPAYMENT_ZTRANSACTION_INDEX` on `ZTRANSACTION`

**ZRECONCILEDTRANSACTION**

- `ZRECONCILEDTRANSACTION_ZFITRANSACTION_INDEX` on `ZFITRANSACTION`
- `ZRECONCILEDTRANSACTION_ZORIGINALTRANSACTION_INDEX` on `ZORIGINALTRANSACTION`
- `ZRECONCILEDTRANSACTION_ZRECONCILERECORD_INDEX` on `ZRECONCILERECORD`

**ZRECONCILERECORD**

- `ZRECONCILERECORD_ZACCOUNT_INDEX` on `ZACCOUNT`

**ZREPORT**

- `ZREPORT_ZFOLDER_INDEX` on `ZFOLDER`

**ZSALESTAXPAYMENT**

- `ZSALESTAXPAYMENT_ZACCOUNTINGRECORD_INDEX` on `ZACCOUNTINGRECORD`
- `ZSALESTAXPAYMENT_ZBUSINESS_INDEX` on `ZBUSINESS`
- `ZSALESTAXPAYMENT_ZSALESTAXAGENCY_INDEX` on `ZSALESTAXAGENCY`
- `ZSALESTAXPAYMENT_ZTRANSACTIONENTRY_INDEX` on `ZTRANSACTIONENTRY`

**ZSALESTAXRATE**

- `ZSALESTAXRATE_ZSALESTAXAGENCY_INDEX` on `ZSALESTAXAGENCY`

**ZSCHEDULEADJUSTMENTENTRY**

- `ZSCHEDULEADJUSTMENTENTRY_ZPARENT_INDEX` on `ZPARENT`
- `Z_ScheduleAdjustmentEntry_byOriginalScheduledDateIndex` on `ZORIGINALSCHEDULEDDATE`
- `Z_ScheduleAdjustmentEntry_originalScheduledDate` on `ZORIGINALSCHEDULEDDATE`

**ZSECURITY**

- `Z_Security_syncID` on `ZSYNCID`
- `Z_Security_ticker` on `ZTICKER`

**ZSECURITYID**

- `ZSECURITYID_ZSECURITY_INDEX` on `ZSECURITY`
- `ZSECURITYID_ZUNIQUEID_INDEX` on `ZUNIQUEID`
- `Z_SecurityID_uniqueID` on `ZUNIQUEID`

**ZSECURITYQUOTE**

- `ZSECURITYQUOTE_ZQUOTEDATE_INDEX` on `ZQUOTEDATE`
- `ZSECURITYQUOTE_ZSECURITY_INDEX` on `ZSECURITY`
- `ZSECURITYQUOTE_ZSECURITY_ZQUOTEDATE` on `(ZSECURITY, ZQUOTEDATE)`
- `Z_SecurityQuote_compoundIndex` on `(ZSECURITY, ZQUOTEDATE)`
- `Z_SecurityQuote_quoteDate` on `ZQUOTEDATE`
- `Z_SecurityQuote_security_quoteDate` on `(ZSECURITY, ZQUOTEDATE)`

**ZSECURITYQUOTEDETAIL**

- `ZSECURITYQUOTEDETAIL_ZSECURITYQUOTE_INDEX` on `ZSECURITYQUOTE`

**ZSECURITYSPLIT**

- `ZSECURITYSPLIT_ZSECURITY_INDEX` on `ZSECURITY`
- `ZSECURITYSPLIT_ZSECURITY_ZSPLITDATE` on `(ZSECURITY, ZSPLITDATE)`
- `ZSECURITYSPLIT_ZSPLITDATE_INDEX` on `ZSPLITDATE`
- `Z_SecuritySplit_compoundIndex` on `(ZSECURITY, ZSPLITDATE)`
- `Z_SecuritySplit_security_splitDate` on `(ZSECURITY, ZSPLITDATE)`
- `Z_SecuritySplit_splitDate` on `ZSPLITDATE`

**ZTAG**

- `ZTAG_ZPARENTCATEGORY_INDEX` on `ZPARENTCATEGORY`
- `ZTAG_Z_ENT_INDEX` on `Z_ENT`
- `Z_Tag_syncID` on `ZSYNCID`

**ZTAXPAYMENTRECORD**

- `ZTAXPAYMENTRECORD_ZBUSINESS_INDEX` on `ZBUSINESS`

**ZTRANSACTION**

- `ZTRANSACTION_ZACCOUNTINGRECORD_INDEX` on `ZACCOUNTINGRECORD`
- `ZTRANSACTION_ZACCOUNT_INDEX` on `ZACCOUNT`
- `ZTRANSACTION_ZACTION_INDEX` on `ZACTION`
- `ZTRANSACTION_ZCHECKPAYCHECK_INDEX` on `ZCHECKPAYCHECK`
- `ZTRANSACTION_ZDOWNLOADSESSION_INDEX` on `ZDOWNLOADSESSION`
- `ZTRANSACTION_ZERROR_INDEX` on `ZERROR`
- `ZTRANSACTION_ZFITRANSACTION_INDEX` on `ZFITRANSACTION`
- `ZTRANSACTION_ZIFSPAYMENT_INDEX` on `ZIFSPAYMENT`
- `ZTRANSACTION_ZLOANACCOUNT_INDEX` on `ZLOANACCOUNT`
- `ZTRANSACTION_ZPLACEHOLDERACCOUNT_INDEX` on `ZPLACEHOLDERACCOUNT`
- `ZTRANSACTION_ZPOSITION_INDEX` on `ZPOSITION`
- `ZTRANSACTION_ZPRESENTMENTBILLERACCOUNT_INDEX` on `ZPRESENTMENTBILLERACCOUNT`
- `ZTRANSACTION_ZQUICKPAYPAYMENT_INDEX` on `ZQUICKPAYPAYMENT`
- `ZTRANSACTION_ZRECONCILEDTRANSACTION_INDEX` on `ZRECONCILEDTRANSACTION`
- `ZTRANSACTION_ZRENAMINGRULEUSED_INDEX` on `ZRENAMINGRULEUSED`
- `ZTRANSACTION_ZSENDACCOUNT_INDEX` on `ZSENDACCOUNT`
- `ZTRANSACTION_ZTARGETACCOUNT_INDEX` on `ZTARGETACCOUNT`
- `ZTRANSACTION_ZUSERPAYEE_INDEX` on `ZUSERPAYEE`
- `ZTRANSACTION_Z_ENT_INDEX` on `Z_ENT`
- `Z_Transaction_enteredDate` on `ZENTEREDDATE`
- `Z_Transaction_postedDate` on `ZPOSTEDDATE`
- `Z_Transaction_quickenId` on `ZQUICKENID`
- `Z_Transaction_syncID` on `ZSYNCID`

**ZUSERPAYEE**

- `ZUSERPAYEE_ZBPSACCOUNT_INDEX` on `ZBPSACCOUNT`
- `ZUSERPAYEE_ZCHECKPAYADDRESS_INDEX` on `ZCHECKPAYADDRESS`
- `ZUSERPAYEE_ZIFSBILLERACCOUNT_INDEX` on `ZIFSBILLERACCOUNT`

**ZUSERPAYEEADDRESS**

- `ZUSERPAYEEADDRESS_ZUSERPAYEE_INDEX` on `ZUSERPAYEE`

The join tables (`Z_15USERTAGS`, `Z_55USERTAGS`, `Z_1BUDGETS`, `Z_1DOWNLOADSESSIONS`) each have multiple indexes with historically-numbered names reflecting Core Data model version migrations. All resolve to the same two-column composite on the current physical table.

---

## Key Relationships

### Transaction to Account

```sql
SELECT t.*, a.ZNAME AS account_name
FROM ZTRANSACTION t
JOIN ZACCOUNT a ON t.ZACCOUNT = a.Z_PK;
```

- `ZTRANSACTION.ZACCOUNT` references `ZACCOUNT.Z_PK`
- Filter by `ZTRANSACTION.Z_ENT` to select only cash flow or investment transactions

### Transaction to Payee

```sql
SELECT t.*, p.ZNAME AS payee_name
FROM ZTRANSACTION t
JOIN ZUSERPAYEE p ON t.ZUSERPAYEE = p.Z_PK;
```

- `ZTRANSACTION.ZUSERPAYEE` references `ZUSERPAYEE.Z_PK`
- Payee is nullable (investment transactions typically have no payee)

### Transaction Split Entries (Categories)

```sql
SELECT t.Z_PK AS txn_id, e.ZAMOUNT, tag.ZNAME AS category_name
FROM ZTRANSACTION t
JOIN ZCASHFLOWTRANSACTIONENTRY e ON e.ZPARENT = t.Z_PK
JOIN ZTAG tag ON e.ZCATEGORYTAG = tag.Z_PK;
```

- `ZCASHFLOWTRANSACTIONENTRY.ZPARENT` references `ZTRANSACTION.Z_PK`
- `ZCASHFLOWTRANSACTIONENTRY.ZCATEGORYTAG` references `ZTAG.Z_PK` (where `ZTAG.Z_ENT` corresponds to `CategoryTag`)
- A single transaction may have multiple split entries (one per category)
- The sum of split `ZAMOUNT` values equals the transaction total

### Category Tag Hierarchy

```sql
SELECT child.ZNAME AS subcategory, parent.ZNAME AS parent_category
FROM ZTAG child
JOIN ZTAG parent ON child.ZPARENTCATEGORY = parent.Z_PK
WHERE child.Z_ENT = (SELECT Z_ENT FROM Z_PRIMARYKEY WHERE Z_NAME = 'CategoryTag');
```

- `ZTAG.ZPARENTCATEGORY` is a self-referential FK to `ZTAG.Z_PK`
- Top-level categories have `ZPARENTCATEGORY = NULL`
- Hierarchy is typically one level deep (parent > child), though deeper nesting is possible

### Transaction to Transfer Target

```sql
SELECT t.*, target.ZNAME AS transfer_to_account
FROM ZTRANSACTION t
JOIN ZACCOUNT target ON t.ZTARGETACCOUNT = target.Z_PK
WHERE t.ZTARGETACCOUNT IS NOT NULL;
```

- `ZTRANSACTION.ZTARGETACCOUNT` references `ZACCOUNT.Z_PK` for the destination of a transfer
- `ZTRANSACTION.ZSENDACCOUNT` may also be set for the source side

### Investment Positions and Securities

```sql
SELECT p.*, s.ZNAME AS security_name, s.ZTICKER, a.ZNAME AS account_name
FROM ZPOSITION p
JOIN ZSECURITY s ON p.ZSECURITY = s.Z_PK
JOIN ZACCOUNT a ON p.ZACCOUNT = a.Z_PK;
```

- `ZPOSITION.ZSECURITY` references `ZSECURITY.Z_PK`
- `ZPOSITION.ZACCOUNT` references `ZACCOUNT.Z_PK`
- `ZSECURITYID` provides alternate identifiers (CUSIP, ticker) via `ZSECURITYID.ZSECURITY`

### Lot Tracking

```sql
SELECT l.*,
       lm.ZAFTERUNITS,
       lm.ZAFTERCOSTBASIS,
       lm.ZBEFOREUNITS,
       lm.ZBEFORECOSTBASIS,
       t.ZPOSTEDDATE
FROM ZLOT l
JOIN ZLOTMOD lm ON lm.ZLOT = l.Z_PK
JOIN ZTRANSACTION t ON lm.ZTRANSACTION = t.Z_PK;
```

- `ZLOT.ZPOSITION` references `ZPOSITION.Z_PK`
- `ZLOTMOD.ZLOT` references `ZLOT.Z_PK` (modifications to the lot over time)
- `ZLOTMOD.ZTRANSACTION` ties each lot change to a specific transaction
- `ZLOTASSIGNMENT` links source and destination transactions for lot transfers

### Account to Financial Institution

```sql
SELECT a.ZNAME, fi.ZNAME AS institution_name, cfi.ZLOGINNAME, cfi.ZDISPLAYNAME
FROM ZACCOUNT a
JOIN ZFINANCIALINSTITUTION fi ON a.ZFINANCIALINSTITUTION = fi.Z_PK
LEFT JOIN ZCLOUDFILOGIN cfi ON a.ZCLOUDFILOGIN = cfi.Z_PK;
```

- `ZACCOUNT.ZFINANCIALINSTITUTION` references `ZFINANCIALINSTITUTION.Z_PK`
- `ZACCOUNT.ZCLOUDFILOGIN` references `ZCLOUDFILOGIN.Z_PK` (cloud aggregation credentials)

### Reconciliation

```sql
SELECT rr.ZENDDATE, rr.ZBEGINNINGBALANCE, rr.ZENDINGBALANCE, rt.ZORIGINALTRANSACTION
FROM ZRECONCILERECORD rr
JOIN ZRECONCILEDTRANSACTION rt ON rt.ZRECONCILERECORD = rr.Z_PK;
```

- `ZRECONCILERECORD.ZACCOUNT` references `ZACCOUNT.Z_PK`
- `ZRECONCILEDTRANSACTION.ZRECONCILERECORD` references `ZRECONCILERECORD.Z_PK`
- `ZRECONCILEDTRANSACTION.ZORIGINALTRANSACTION` references `ZTRANSACTION.Z_PK`

---

## Important Notes

### Shared Physical Tables (Entity Type Inheritance)

Core Data uses **single-table inheritance** for several entity hierarchies. The `Z_ENT` column discriminates subtypes:

| Physical Table | Entity Types | Notes |
|---|---|---|
| `ZTRANSACTION` | Transaction, CashFlowTransaction, SmartCashFlowTransaction, InvestmentTransaction | Investment-specific columns (`ZUNITS`, `ZCOMMISSION`, `ZPLACEHOLDERCOSTBASISUNITS`, etc.) are NULL for cash flow rows. Per-unit price is not stored directly — derive it from `ZAMOUNT` ÷ `ZUNITS`, or look up `ZSECURITYQUOTE.ZCLOSINGPRICE` for the trade date. |
| `ZTAG` | Tag, CategoryTag, UserTag, CashFlowTag | Filter on Z_ENT to get only categories vs. user tags |

### Z79_PARENT Artifact

The `ZTAG` table contains a column `Z79_PARENT` alongside `ZPARENTCATEGORY`. This is a Core Data artifact from model migrations -- the `79` in `Z79_PARENT` references a historical `Z_ENT` value for the `CategoryTag` entity. In practice, use `ZPARENTCATEGORY` for the parent-child category relationship.

### Signed Amounts

- `ZTRANSACTION.ZAMOUNT` -- credits are positive, debits are negative (from the account's perspective)
- `ZCASHFLOWTRANSACTIONENTRY.ZAMOUNT` -- split amounts follow the same sign convention
- Investment transactions use `ZTRANSACTION.ZUNITS` (can be negative for sells); per-unit price is not stored as a column — derive it from `ZAMOUNT` ÷ `ZUNITS`, or join to `ZSECURITYQUOTE.ZCLOSINGPRICE` for the trade date

### Date Fields

- `ZTRANSACTION.ZPOSTEDDATE` -- the date the transaction posted; **can be NULL** for pending/scheduled transactions
- `ZTRANSACTION.ZENTEREDDATE` -- when the user entered or the system downloaded the transaction; always populated
- Both are Core Data timestamps (seconds since 2001-01-01)

### Account Type Values

The `ZACCOUNT.ZTYPENAME` column stores account types as **uppercase strings**:

- `CHECKING`, `SAVINGS`, `CREDITCARD`, `CASH`
- `INVESTMENT`, `RETIREMENT` (brokerage/IRA accounts)
- `MORTGAGE`, `LOAN`, `LIABILITY`
- `ASSET`, `PROPERTY`

### ZTAG Multi-Entity Usage

The `ZTAG` table serves triple duty:

1. **CategoryTag** -- income/expense categories (Groceries, Rent, Salary). Has parent-child hierarchy via `ZPARENTCATEGORY`.
2. **UserTag** -- user-created freeform tags. Linked to transactions via the `Z_15USERTAGS` join table.
3. **CashFlowTag** -- system-internal tags for cash flow classification.

Always filter on `Z_ENT` (looked up from `Z_PRIMARYKEY`) when querying `ZTAG` to avoid mixing entity types.

### Quicken Database Access

- The database file is located at `~/Documents/<name>.quicken/data`
- Quicken **encrypts** the file when the application is not running; the `data` file becomes a ~24KB metadata stub
- The file's inode changes when Quicken opens/closes the database
- Always open the database in **read-only mode** (`?mode=ro`) to avoid interfering with Quicken's Core Data stack

